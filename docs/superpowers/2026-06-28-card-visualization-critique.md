# Flight dashboard — visualization critique & new-stat ideas

_Generated 2026-06-28 by a 10-thinker adversarial panel (Tufte purist, Spotify-Wrapped, EDA statistician, data journalist, Bloomberg-density, cartographer, ESPN sports-stats, interaction architect, quantified-self, generative artist) → per-card synthesis → ranked ideas + completeness critic. Sample = the full real export (1,800 flights, 1991–2026)._

## Per-card verdicts

### REPLACE (6)

#### Close to home (state) `[domesticState]` — replace · effort M
**Recommended:** Replace the standalone domesticState bar-list with ONE merged "How far from home" card: a single horizontal 100%-stacked proportion bar over the four mutually-exclusive shells the engine already produces — In-state (Texas) / In-country / In-continent / Intercontinental — sized by flight count, with count + % labeled inline (or in a compact legend for thin segments). Above it, a hero headline auto-derived from the dominant segment, e.g. "64% of your flights never left Texas." Below it, a segmented control (State | Country | Continent | World) that swaps the same BarList route breakdown that each of the 4 cards renders today; each stacked segment is itself clickable, opening the existing flight overlay (reuse flightsByDomesticTier / flightsIntercontinental). This collapses 4 near-identical cards (domesticState, domesticCountry, domesticContinent, intercontinental) into 1, makes the nesting visible, and surfaces the single stat that matters. Encoding: x = proportion of total resolved non-local flights (the bar IS the whole; the four tiers partition it exactly per classify.ts), color = the 4 existing tier accents (orange/green/blue/purple), direct count+% labels (no hover-only), segmented control for drill, segment-click -> flight overlay.

**Alternatives:** 
- Keep it as a single annotated proportion bar but DON'T add the segmented control merge — just convert the lone domesticState card from a route ranking into one in-state-vs-rest 100%-stacked bar with the '% never left Texas' headline, and leave the other 3 cards alone. Lower effort, still kills the part-to-whole-as-ranking mistake, but leaves the 4-card redundancy.
- Concentric rings / nested donut (center = in-state, outer ring = intercontinental), each arc sized by flight share. Visually striking and literally 'rings of home,' but angular/area proportions read less accurately than a flat bar, exact % is harder to label, and it weakens the per-tier route drill-down — the owner favors legible over novel, and dislikes the more decorative existing viz modes.
- Small ordered 4-row count list with an inline proportion micro-bar per row plus a single total — Bloomberg-dense fallback if the stacked bar's thin intercontinental segment proves unreadable; keeps exact numbers scannable but loses the one-glance part-to-whole punch.

**Why:** This card is fundamentally a part-to-whole over a single total, not a ranking — classifyRoute() assigns every resolved non-local flight to exactly ONE of these four concentric shells (intercontinental > intra-continent > intra-country > intra-state), so they sum cleanly to 100% and a 100%-stacked bar is the honest, native encoding. A ranked bar-list of in-state routes buries the actual story; the real signal in his export is the share, and it's dramatic: with DFW/DAL/AUS/HOU/SAT all in Texas and ~64% Texas concentration, the in-state segment will visibly dominate, making "X% never left Texas" a true, screenshot-worthy reveal rather than manufactured drama. Merging the 4 sibling cards removes genuine redundancy (4 visually identical bar cards for one nested concept) and makes the geographic nesting — currently invisible across separate cards — explicit and navigable. It stays in the owner's stated comfort zone (he likes the route-arc map, dislikes heatmaps/choropleths): a flat labeled bar is glanceable and not gimmicky. Critically, it preserves every drill-down he already has — the engine fns (superDomestic, intercontinentalByPair, flightsByDomesticTier, flightsIntercontinental) and the BarList route breakdowns all survive behind the segmented control and clickable segments.

#### Close to home (country) `[domesticCountry]` — replace · effort M
**Recommended:** Replace this card (and its two `superDomestic` siblings) with ONE unified "Close to home" card: a single horizontal 100%-stacked proportion bar over the full home-proximity ladder the engine already produces — intra-state → intra-country → intra-continent → intercontinental (4 segments, ordered near→far, reusing the existing per-tier accents orange/green/blue/purple). Direct-label each segment inline with its % and count where it fits; small segments get their value in the legend row beneath rather than a cramped on-bar label. A short dek above states the headline read, e.g. "X% of your flying never leaves Texas; only Y% crosses an ocean." Each segment is click-through: clicking opens the flight-list overlay for that tier (handlers already exist — `flightsByDomesticTier` for the three domestic tiers, `flightsIntercontinental` for the far end), so the current top-routes BarList becomes the drill-down target instead of the always-on body. This keeps the binary domestic-vs-international read the prompt cares about (intra-state+intra-country+intra-continent vs the intercontinental segment) while honestly placing it on the real 4-rung ladder. Encoding: x = share of total flights (0–100%), color = tier, segment width = proportion; tabular-num % labels; legend row carries raw counts for power-user glanceability.

**Alternatives:** 
- Secondary toggle on the SAME card: a thin per-year 100%-stacked strip (year on x, tier-share stacked to 100% on y) surfacing the owner's actual 'increasingly international' arc — Strava/Tufte/journalist personas converged here, and the engine already has per-flight year + tier, so this is the one trend the static lifetime split buries. Ship it as a 'by year' QuickToggle, not a second card.
- Concentric 'rings of home' donut (intra-state innermost → intercontinental outermost), the radial framing the creative-coding/ESPN personas pushed. Striking and screenshot-worthy, but a stacked bar reads proportions more accurately and is far cheaper; keep the donut only as a later delight beat.
- Minimal fallback if merging the siblings is out of scope this pass: convert just THIS card's body from a route count-list to a single 2-segment 100%-stacked bar (this-country-domestic vs everything-else) with both % direct-labeled, keeping the route BarList as drill-down. Honest binary part-to-whole, least churn.

**Why:** classifyRoute emits a clean 4-level ordinal ladder and the repo ALREADY ships four near-identical sibling cards (domesticState/Country/Continent + intercontinental) from one factory — so the real defect isn't this card's chart type, it's three redundant route-lists fragmenting one part-to-whole story across the grid. A binary/ordinal part-of-whole is the canonical case for a single 100%-stacked bar, not a two-row count list: the eye reads the ratio instantly and the owner's signature shape (heavy Texas concentration with a recent climb into international) becomes legible in one figure. It serves the prompt's domestic-vs-international intent (near-three segments vs the far segment) without lying about the ternary/quaternary structure the data actually has. It respects his explicit dislikes — no choropleth, no heat/density, no calendar heatmap — just an honest proportional bar with drill-down, exactly the 'glanceable + drill-downable over novelty' bar in the brief. 8 of 9 panelists converged on folding the domestic split into one nested proportion figure; the engine data and existing click-through handlers make that cheap and truthful.

#### Close to home (continent) `[domesticContinent]` — replace · effort M
**Recommended:** Do not ship this as a standalone card. Merge it as the third segment of ONE unified "Close to home" card: a single horizontal 100%-stacked proportion bar of the four mutually-exclusive home-range shells, left-to-right by increasing distance: Same state (Texas) | Same country (US) | Same continent (N. America) | Intercontinental. The code already proves these are a strict nested hierarchy (classify.ts: continent->country->region->same), so a part-of-whole encoding is the truthful geometry. Encoding: one full-width bar, four segments width = share of resolved flights, each segment colored with the existing per-tier accents (state #ff7a14, country #12c08a, continent #1aa9ff, intercontinental #6a3cff). Inline-label only segments wide enough to fit ("Same state 71%"); show the rest in a compact legend row beneath with count + percent. Make each segment a click target that opens the existing flights overlay (flightsByDomesticTier / flightsIntercontinental already exist) -> reuses the on-a-map drill-down. Keep route-level detail via BarList subRows (already supported, see intercontinentalCard): clicking a segment can expand its top routes, so nothing the three cards show today is lost. Headline the card with the one true sentence this data tells: e.g. "[X]% of your flights never leave Texas." This collapses 3 near-identical bar-list cards into 1 glanceable, drillable story and frees two grid slots.

**Alternatives:** 
- If the merge is deferred and the card must stay standalone: keep it but reframe from a ranked route list to a single proportion bar (same-continent vs everything else) with inline percent + flight count, matching the Tufte/consumer-designer note. A bare 2-category bar list wastes a row per value.
- Concentric 'rings of home' radial (nested donut: innermost = same-state, outer rings = country/continent/intercontinental) with click-to-drill. More memorable and on-brand for the Pop theme, but harder to label cleanly and to read exact shares than a stacked bar; the owner dislikes decorative encodings (heatmap/choropleth), so favor the legible stacked bar over the radial unless delight is the priority.
- Icicle / nested-proportion bar (each tier a stacked block whose width is its share, drilling into routes within), which literally encodes the Russian-doll nesting; good middle ground but more build effort than the flat stacked bar.

**Why:** In the real export this tier (same-continent = intra-continent: US<->Canada/Mexico) is thin, while same-state is enormous (64% of endpoints are 5 Texas airports; DFW=965, DAL=492, AUS=353). A standalone ranked route list of a handful of cross-border routes is low-signal real estate whose only meaning is "how it compares to the bigger shells" - which a standalone card structurally cannot show. The four tiers are mathematically one mutually-exclusive part-of-whole (verified in classify.ts), so the honest, glanceable encoding is one proportional bar, not three look-alike lists the user must mentally stitch. 7 of 8 panelists converge on merging; the lone "keep" concedes low value. The merge also matches the owner's demonstrated taste: he rejects low-information encodings (calendar heatmap, density/choropleth map) and likes concrete, drillable views (route-arc map) - a labeled proportion bar with click-through to the flights overlay is exactly that.

#### When you fly `[whenYouFly]` — replace · effort S
**Recommended:** ONE shared-axis 24-hour chart that overlays departures and arrivals, plus a one-line derived persona caption. Concrete encoding: a single horizontal 0-23h axis (replace the two stacked HourHistograms with one chart). Draw departures and arrivals as two thin filled areas/lines in two distinct accent hues (e.g. var(--accent-6) for departures, a contrasting accent for arrivals), bar/area length = count, on a SHARED y-scale so the two distributions are directly comparable. Light vertical reference band behind 8am-6pm ("business hours") for context. Direct-label each series' peak inline (departures peak ~6pm, arrivals later-evening) instead of a separate legend. Caption is a single computed sentence: "You're an evening flyer - most departures 4-8pm." X ticks at 12a/6a/12p/6p (reuse existing tick logic). CRITICAL fixes carried over: drop the opacity-encodes-count double-encoding (height/length already encodes it - this is redundant ink), and make each hour bar/cell clickable -> overlay?.openFlights('Departures at 6pm', flights filtered by that hour) using the existing Overlay drill-down pattern that every other card already has and this card currently lacks. Honesty note to bake into code comments: depHourLocal and arrHourLocal are in DIFFERENT local frames (origin-local vs destination-local), so the caption should say "depart / land at local time" - do NOT imply a single elapsed-flight rhythm.

**Alternatives:** 
- Radial 24-hour clock (polar histogram): hours around the dial, departures as one colored ring and arrivals as a concentric second ring, bar length = count. This is the panel's single most-loved form (artist + 4 others) and maps perfectly to time-as-a-cycle (fixes the 0->23 wrap-around break, e.g. near-empty 0-4am red-eye reads as a dark arc). Choose this over the linear overlay if you want the shareable 'lit-up evening clock' hero - it is strictly the more delightful object; the only reason it is the alternative not the pick is slightly higher D3 effort (arc/polar math) and that a clock's radius is a known-mild perceptual encoding vs a clean shared linear axis.
- Mirrored back-to-back (butterfly) chart: departures growing up from a center axis, arrivals growing down, shared 24h x-axis. Reads the daily out-and-back shape as one silhouette and is cheap, but the linear overlay is more directly comparable for two series that aren't truly opposites.
- Keep TWO charts but at minimum strip the redundant opacity encoding and add per-bar click-through to flights. This is the do-nothing-brave floor if overlay work must be deferred - but it leaves the core complaint (serial comparison of two separated histograms) unfixed, so it is not recommended as the endpoint.

**Why:** The whole point of this card is dep-vs-arr timing, and that comparison is only legible on a shared axis - the current two stacked histograms force the eye to diff across vertical space, which is exactly why 7 of 8 panelists (including the Tufte purist who voted replace) converged on a single overlaid encoding. The owner's data supports the payoff cleanly: depHourLocal/arrHourLocal are pre-binned 0-23 ints with good coverage, so an evening-departure persona is a true, earnable reveal. I deliberately did NOT adopt the 24x7 punch-card / weekday-x-hour heatmap that several panelists pushed: the owner has explicitly called the calendar HEATMAP and the map density HEAT useless, and a punch-card is the same heatmap-family object - high risk of the same rejection. Day-of-week (Mon-Thu business signature, dead Saturday) is genuinely the loudest unplotted signal, but it answers a different question than 'hours of day' and belongs in a temporal/by-time card, not bolted onto this one. The two concrete missing-quality fixes - kill the redundant opacity double-encoding and add the openFlights drill-down that every sibling card already has - make this card honest and explorable instead of a static read, matching the rest of the dashboard.

#### Travel intensity `[intensity]` — replace · effort M
**Recommended:** A flights-per-YEAR vertical bar/area column chart (the career arc), x = year 1991-2026, y = flight count (linear, shared axis), bars colored in accent-3 with opacity scaling kept only as a subtle secondary cue. Direct-label 2-3 annotated callouts on the curve itself: "Peak: 245 (2016)", "COVID: 76 (2020)", "COO era - fewer, longer, intl". Click a year bar -> drill into that year's flights (reuse flightsByYear) AND set the global year scope dropdown so this card becomes the dashboard's time navigator. Render below the bars a thin per-year monthly micro-sparkline strip (12 tiny cells/bars per year, from byYearMonthMatrix) so the month seasonality detail the heatmap tried to show survives, but as a secondary layer that doesn't fight the main signal. Position/length on a shared axis (the strongest perceptual channel) replaces opacity (the weakest).

**Alternatives:** 
- Connected line/area of flights-per-MONTH across the full span (continuous byMonth series, ~420 points) with year gridlines and the same 3 annotations - reads as one unbroken trajectory and exposes the COVID cliff sharply, but is busier and harder to drill per-year.
- Horizon chart or ridgeline: one band per year of monthly counts. Compact and dense, but perceptually harder for a non-power-user owner to read precisely and the year-arc (the actual hero signal) is less immediate than plain bars.
- Year-bars (primary) + keep the existing month heatmap collapsed behind a 'show monthly detail' toggle - lowest-regret path that preserves the original creative artifact without it being the default.

**Why:** unused

#### Aircraft class `[aircraftClass]` — replace · effort M
**Recommended:** Replace the 4-row BarList with a single 100%-stacked horizontal proportion bar (one full-width bar split into segments: Narrowbody | Regional | Widebody | Prop, with Unclassified folded to the end or dropped). Encoding: x = share of all flights (segment width ∝ count), color = the card accent ramp (dominant narrowbody = strongest accent #ff3d57, the rest stepped/desaturated so the eye reads dominance, not a rainbow). Direct-label inside or just-above the bar with a headline stat: "Narrowbody traveler — ~X% single-aisle (N flights)"; label each segment inline with count + % when it's wide enough, and put the thin segments (wide/prop) in a tiny legend row beneath. CRITICAL: keep every segment clickable, calling the same flightsByAircraftClass(...) → ctx.overlay.openFlights(...) it uses today, so drill-through and click-through parity with the rest of the dashboard is preserved. Keep the per-type drill-down (737-800, A321, MD-80, CRJ…) but move it BELOW the share bar as a collapsible "see types" expander rather than as the primary view, so the headline ratio reads first and the box-score detail is one tap away.

**Alternatives:** 
- 100-square waffle (10x10 grid) colored by class — same part-to-whole, more 'screenshottable'/Wrapped-y and makes 'X of 100 of your flights were narrowbody' visceral; costs more pixels and slightly more build than the bar, and thin classes (1-2 squares) get hard to click, so it's the delight-forward second choice.
- KEEP-but-merge: fold this into the Aircraft card as a small stacked-share bar in its header (the consumer-delight + creative-coding voters' note that it conceptually overlaps the manufacturer card). Tightens the grid and pairs the share-bar directly above the brand→family→type icicle/bars; deferred only because it touches two cards and the registry.
- Minimal keep: leave the BarList but collapse to a single stacked bar visually by sorting + adding a % column and a thin inline share track per row. Lowest effort, but it's the weakest — it keeps the 'mentally sum 4 bars' problem the panel flagged.

**Why:** The data is a textbook part-of-whole: ~4-5 mutually-exclusive body classes that partition all 1,800 flights, and they are extremely lopsided — 737-800/700 (~740) + A321 (242) + MD-80s make narrowbody the overwhelming majority, regional jets (CRJ/Embraer) a clear second band, with widebody and prop as slivers. A 4-row ranked BarList forces the viewer to mentally add the rows to recover the one number that matters (share single-aisle) and wastes vertical space on what is essentially one ratio. A single 100%-stacked bar shows that dominance instantly, in one glance, in far less height — exactly what a power-user dashboard wants. Six of eight panelists (Tufte, NYT-Pudding, Bloomberg, ESPN, IA, creative-coder) explicitly converge on the stacked proportion bar; the two 'keep' votes (Wrapped designer, Strava) both concede a stacked bar would be tighter and is the honest encoding, and the Strava note that it's 'not temporal' confirms the owner's hated time-based encodings (calendar heatmap, density map) aren't relevant here. Keeping the segments clickable and the per-type drill below preserves the one genuinely good property of the current card (nested drill-down into the dead-MD-80 / 737 detail) while fixing the wrong top-level encoding. This is the panel converging on something clearly better, not novelty — it's the most boring-correct chart for this exact shape.

### IMPROVE (15)

#### Your map `[map]` — improve · effort M
**Recommended:** Make the route great-circle arc map the SINGLE mode and turn it into the dashboard's spatial drill-down surface. Concretely: (1) DELETE the Heat and Choropleth toggles entirely — owner-rejected and cartographically dishonest (US-as-one-red-blob MAUP; the SVG-blur "heat" is a glow, not a real KDE, and ignores projection area). That removes the 3-button toggle row and its CAPTION map. (2) Keep geoNaturalEarth1 + great-circle arcs; keep the existing log-ish arc width/opacity scaling (WorldMap.tsx L98-99) but switch arc opacity to a true log scale so the DFW/DAL/AUS/HOU/SAT Texas bundle (~62% of endpoints) doesn't burn out the canvas while the 29 once-visited airports stay visible as hairlines. (3) Size airport dots by sqrt(visit count) instead of the current fixed r=3 (L158) — this is the strongest unused channel and instantly shows the 5 mega-hubs vs the long tail; render the home airport (DFW, from ctx.settings) as a single larger filled+ringed anchor dot. (4) WIRE DRILL-DOWN, the biggest miss: the map is the only card in an overlay-driven dashboard with zero click handlers. The per-route and per-node geometry already exists in the useMemo, and overlay.openFlights + flightsByRouteKey + flightsByAirportKey already exist and are used by RoutesCard/AirportsCard. Add onClick on each arc → overlay.openFlights(routeName, flightsByRouteKey(...)), onClick on each dot → overlay.openFlights(airportName, flightsByAirportKey(...)), pointer cursor + hover highlight, and a tooltip showing route/airport name + flight count (+ total miles for routes). (5) Add a tiny arc-width legend (e.g. 1x / 20x / 234x flights) so the encoding is readable. Honor the existing groupAirports setting (already passed in). Net: one honest spatial encoding, frequency on width+dot-size, and every arc/dot becomes a doorway into the flight list — consistent with every other card.

**Alternatives:** 
- Add an OPTIONAL 'Replay' button (not a peer mode, not autoplay): arcs draw on in chronological order over ~6s with a 1991→2026 year counter and DFW pinned/glowing, freezing on the full web. High delight / Spotify-Wrapped screenshot energy, but it's novelty over glanceability and adds animation/RAF complexity, so it stays a small secondary affordance behind the static drill-down map — only worth it once the core improve ships.
- Add a directly-labeled annotation layer (NYT/Pudding style) calling out the 1-2 arcs that ARE the story: 'DFW–AUS, flown 234× — your most-repeated leg' and 'farthest: DEL, ~8,000 mi' with leader lines, plus a one-line dek above the map. Cheap, story-first, pairs well with the recommendation; risk is label clutter in the dense Texas triangle, so cap at 2-3 callouts.
- Cartographer's force-directed / hierarchical edge bundling so the hundreds of Texas-hub arcs braid into legible corridors instead of a hairball. Technically the most 'correct' fix for hub overplotting, but it's the highest-effort/highest-risk option (custom bundling math in a single-file app, can distort true geography) and log-opacity + sqrt dots already de-hairball most of it — defer unless the hub still reads as mush after the core change.

**Why:** Effort is M, but at the low end: deleting the two modes and the toggle is pure subtraction; sqrt dots, log opacity, the home anchor, and the legend are small local edits to the existing useMemo/SVG in WorldMap.tsx; and drill-down reuses overlay.openFlights + flightsByRouteKey/flightsByAirportKey (already proven in RoutesCard/AirportsCard) — the per-route/per-node identities just need to ride along on the arc/dot objects instead of being discarded. The only genuinely new bits are an SVG hover tooltip and hover highlighting. It's not S only because of the SVG hit-testing/tooltip plumbing and updating world-map.test.tsx for the removed modes and new click handlers.

#### Overview `[overview]` — improve · effort S
**Recommended:** Keep the hero-number grid (84px gradient Flights + Distance + the 2x2 tabular-nums grid) as the spine — it is the correct encoding for lifetime scalars and is already the most "Wrapped" card in the app. Add two cheap, additive things, both reusing primitives that already exist in the repo:

1) A full-width flights-per-year SPARKLINE directly under the big Flights number. Encoding: a thin line + faint same-hue area fill (accent #ff3d57 at ~12% alpha) across the ~23 active years (1991-2026); x = year on a LINEAR scale over min..max (so the sparse pre-2008 years read as a flat low tail, not one fat bar each); y = flight count; no axes, gridlines, or ticks. Direct-label only two points: a dot+caption at the peak year ("245 in 2016") and at the latest year ("44 in 2026"). Leave the 2020 COVID notch unlabeled — the visible dip IS the payload. ~56-64px tall, full card width. Data is free: derive from model.scoped via the existing byYear() aggregator — every flight already carries .year and model.years already exists, so NO engine change is needed. Build it as a small inline-SVG <Sparkline> (or extend the existing HourHistogram flex-bar pattern); do not pull in a heavy D3 axis.

2) Make every tile a DRILL target, fulfilling the app's "everything is clickable" promise (currently these are the only inert stats in the dashboard, and Overview is the first card people tap). Wire each through the existing overlay.openFlights(title, flights[]): Flights -> model.scoped; Distance -> scoped sorted by distance; Time in flight -> scoped sorted by duration; Unique airports/Airlines/Unique routes -> their existing popups. Add cursor:pointer and a small hover ↗ affordance, exactly as AirportsCard already does on its title and rows.

Also add ONE human-anchor caption under Time in flight — "that's N full days at 35,000 ft" (fmtDurationDays already yields days) — to land the emotional/journalistic context the Wrapped and NYT-Pudding personas asked for, at zero layout cost. Keep tabular-nums, kill nothing structural.

**Alternatives:** 
- Sparkline ONLY (no drill-down): add the flights-per-year line under the Flights number and stop there. Smallest possible change, captures the panel's strongest convergence (6 of 9 want the per-year trend), but leaves the inert-tile mental-model bug the interaction architect flagged.
- Two micro-sparklines instead of one: a flights/yr line under Flights AND a miles/yr line under Distance (Tufte/Bloomberg's literal ask). Slightly more ink for marginal gain — the two series are near-collinear since miles track flights, so the second line mostly repeats the first; not worth the clutter on a glance-first card.
- Pure keep + one annotated dek (journalist's minimal version): leave the grid untouched and add a single subhead under Flights — 'across 23 years; half of them in 2015-19.' Honest and near-zero effort, but a sentence cannot show the 2020 cliff or the recent intl pivot the way a 64px sparkline does for the same space.

**Why:** Lifetime totals are genuinely single scalars, so the big-number grid is the right base encoding — the cartographer, ESPN, and Wrapped personas are correct that this should stay a scoreboard, not become a chart. But a lone total LIES by omission here: this owner's data is defined by a boom-bust arc (sparse pre-2008, peak ~245/yr in 2015-19, a hard 2020 COVID crash to 76, then a plateau and intl pivot), and "1,800 flights" flattens all of it. A per-year sparkline is the lowest-ink way to restore that shape, and it is exactly the metric-vs-time encoding the Strava persona names as the dashboard's single biggest miss — while sidestepping every viz the owner explicitly dislikes (calendar heatmap, density map, choropleth). The drill-down fix addresses the one other unanimous-quality gap: Overview is the first card tapped, and inert tiles teach the wrong "nothing here is clickable" model for the whole app. Both changes are additive (no card added, no layout torn out), serve the owner directly, and the panel converges on them (6 of 9 want the sparkline; the IA wants the drill). I down-weighted the pure-keep votes because they implicitly accept a number that hides its own best story, and I rejected the scrollytelling reveal as novelty that fights a glance-first summary.

#### Around the world `[odometer]` — improve · effort M
**Recommended:** Keep the giant "N× around the Earth" hero (it's the screenshot-grade flex and tests already lock it in), but anchor it on an honest axis by adding a cumulative-distance area chart beneath it. Encoding: x = time (first flight date → today, linear), y = cumulative miles (linear, 0 → total), filled area under a single line that climbs from 1991 to 2026. Overlay faint horizontal gridlines at each Earth-lap multiple (24,901 mi) so you literally watch the laps stack, and drop a small flag marker on the curve at each lap-completion point plus the Earth→Moon mark (238,900 mi). Below the chart, replace the static Moon % box with a thin progress bar to the NEXT milestone with a "distance to go" number (e.g. "Moon trip 38% — 148k mi to go"), the one element nearly every persona converged on. Make each milestone flag (and the hero line) clickable to openFlight() the flight that crossed it — reuse the existing milestones()/overlay.openFlight pattern from RecordsCard verbatim. Keep total miles as the footer. This card becomes the dashboard's one true metric-vs-time view, which the owner's data has been missing.

**Alternatives:** 
- Proportional progress ribbon (no time axis): one long horizontal track, current total as a glowing fill, milestone ticks (equator ×N, Moon %) placed at TRUE proportional positions. Simplest honest-magnitude option (S effort) and satisfies Tufte/creative-coder, but throws away the 35-year shape the Strava persona rightly flags as the biggest win — fall back to this only if the area chart proves too cramped in the card's width.
- Keep current card essentially as-is but add only the next-milestone progress bar + click-through-to-crossing-flight, no chart. This is the minimal, near-unanimous panel ask (the journalist/Wrapped/ESPN/IA personas would each accept it) and is S effort — a safe floor if time is tight, but it leaves the time-series gap unaddressed.
- Number-line with tick milestones and a filled progress bar to current total (Tufte's pick): equator, Moon etc. as labeled ticks on one linear horizontal axis. Honest and glanceable, but functionally a 1-D version of the ribbon; the 2-D cumulative curve dominates it by also showing rate-of-travel over a working life.

**Why:** Cumulative distance is the purest odometer metaphor and it is inherently a time series, so plotting it against time is the rare move that satisfies both panel camps at once: the Tufte/creative-coder/Strava "put magnitude on an honest axis" purists AND the Wrapped/ESPN/journalist "give me an emotional, brag-worthy reveal" crowd. The owner's actual data makes the curve worth drawing — a 35-year arc that ramps hard 2013-14, peaks 2015-19, visibly craters at COVID 2020, then plateaus — so the area chart shows the around-the-Earth laps stacking up AND tells the life-shape story, instead of burying one scalar in prose. It directly fills the dashboard's biggest documented miss (it almost never plots a metric against time) while sidestepping every viz the owner explicitly rejected (calendar heatmap, map density, choropleth) since this is a clean line, not a density grid. The data and plumbing already exist: f.distanceMi + f.date give a one-pass cumulative series, and the milestones()/overlay.openFlight() click-through from RecordsCard drops in for the "drill to the flight that crossed this lap" behavior, making the card honest, glanceable, AND drill-downable rather than novelty for its own sake.

#### Most-visited airports `[airports]` — improve · effort S
**Recommended:** Keep the ranked horizontal bar list (correct, honest encoding for a steep-Pareto ranked count; panel converges 7-of-9 on keeping bars), but make four targeted fixes that all reuse the existing BarList + CardFrame + Overlay machinery:

1) DOMINANCE DEK (the missing sentence). Add a one-line dek under the eyebrow, computed live: "Your top 5 are all Texas." plus a dominance ratio (e.g. "DFW alone is ~24% of all your airport touches" or "more than your next 3 airports combined"). Direct-label the story instead of leaving it to be read off bars. Highest-value, near-zero-risk change (NYT/Pudding + Wrapped + journalist all asked for it).

2) HOME HERO stays, gets the ratio attached. The card already pins DFW in a highlighted "Home base" strip with count + click-through (the hero the Wrapped designer wanted) — keep it, append the share sentence (e.g. "≈24% of all your landings").

3) LONG-TAIL ROLLUP in the in-card top-5 AND the See-all popup. After the visible bars, render one muted clickable summary row: "+156 more airports · 29 visited once" that opens the full list / those flights. Kills the dishonest-by-omission complaint without 161 near-empty bars. Reuse BarList's existing sub/footer affordance; no new component.

4) SQRT (not log) BAR-WIDTH SCALE for the See-all popup only. In the popup the range is 965→1, so linear bars make ranks 6-161 hairlines. Add a scale:'sqrt' option to BarList that transforms only bar WIDTH (value/label text stays the literal count) so ORD(67) and a 1-visit airport are distinguishable. sqrt is gentler/more honest than log for counts that include 1s. Keep the in-card top-5 LINEAR (those five are all 200-965 and compare fine). Add a small "even out small bars" toggle in the popup header so the transform is disclosed, never silent.

ENCODING SUMMARY: horizontal bars, length = count (linear in-card, sqrt-width in See-all popup), home/DFW pinned + highlighted accent, flag glyph + metro display name per row, right-aligned tabular count, rows click through to that airport's flights, plus a dek sentence and one rolled-up long-tail row. Do NOT add a proportional-symbol map (overplots the Texas cluster — cartographer's explicit warning) and do NOT add per-year sparklines now (needs new per-airport-per-year engine aggregation not currently plumbed; defer).

**Alternatives:** 
- Bloomberg/ESPN variant: per-row mini sparkline of visits-by-year inside each bar row + a '% of all endpoints' numeric column, to expose base-shift signals (is DAL rising as DFW falls?). Best 'L-effort' upgrade, but requires a NEW engine aggregate (byAirportByYear) that isn't computed today — that, not the UI, is the cost. Defer to a follow-up.
- Texas-vs-Rest toggle (ESPN): a segmented control that splits the list into a 'Texas wall' group vs an 'Away' group, or filters to non-Texas so the rank 6-161 battle reads at linear scale with no transform. Cheaper than sparklines, honest, reuses existing filtering — a fine substitute for the sqrt toggle if a scale transform feels too clever.
- Lollipop / dot-on-a-line instead of filled bars in the See-all popup: dots at sqrt-positioned x for 161 airports read cleaner than 161 thin filled bars and make tail density legible. Pure cosmetic swap; lower priority than the dek + rollup.

**Why:** The data is an extreme single-peak Pareto (DFW 965; top-5 all Texas at ~62-64% of endpoints; then a cliff to ORD 67 and a 29-airport once-only tail across 161 airports). For "rank me by count," bars are the textbook-correct, glanceable, drill-downable encoding — which is why 7 of 9 experts (including the cartographer who'd normally push a map, and the Strava designer whose hero is time) voted keep; a replace would be novelty for its own sake. The card already nails the hard parts: home pinned, accent highlight, click-through rows, See-all popup. The genuine, panel-wide defects are narrow: (a) the story is plotted but never SAID — the Texas monopoly and DFW's ~24% share are the headline and should be a direct-labeled dek; (b) the 965-vs-1 dynamic range makes the long-tail / See-all view dishonest-by-omission (hairline stubs), fixed by a sqrt bar-width transform plus one rolled-up '+156 more · 29 once-only' row. These match the owner's known tastes: he likes the route-arc map and dislikes density/heat encodings, so the fix stays a clean ranked list, not a new spatial viz, and adds the concrete superlative ('top 5 all Texas') his favorite cards trade in. Sparklines are the right ambition but are gated on engine work not yet present, so they're an alternative, not the recommendation. All four recommended fixes are S-effort given the existing BarList sub-row/footer support and the already-sorted model.byAirport.

#### Top routes `[routes]` — improve · effort M
**Recommended:** Keep the ranked horizontal bars + count/miles toggle as the spine (it is the correct encoding for a top-N ordered magnitude list, and the panel's three "replace" voices each break the one thing this card does well: exact comparison of discrete routes). Make three additive changes, in priority order. (1) HERO LINE above the list: a single bold sentence for the runaway #1 route — "AUS <-> DFW · 234 flights · 13% of everything you've ever flown" — with a tiny inline 2px great-circle arc glyph. This is the genuine headline (one route = 13% of a 35-year flying life) and the bar list currently flattens it into row one of ten. (2) DIRECTIONAL SPLIT inside each bar: render each route bar as a centered diverging bar, outbound (a->b) left of center, return (b->a) right, so the near-perfect out-and-back symmetry is visible (DFW->AUS 119 / AUS->DFW 115; Dallas departures 730 vs arrivals 727, ~0.4% off). This surfaces a real, surprising finding the undirected count literally hides, and the engine already supports it for free — routeKey() produces directional `a->b` keys when settings.explicitlyUnique is set, so no new data pipeline is needed. Add a one-line dek: "You always come home — out and back match within ~1% on every corridor." (3) Keep per-row drill-through, "Group metros" live re-rank, and "See all"; optionally split the direction inside the existing flight popup. Encoding summary: position-on-aligned-axis (rank) = primary; bar length = selected metric (count|miles); left/right of center = direction; faint number on right = avg miles or flight count (already present as sub-label).

**Alternatives:** 
- Minimal version (effort S): keep bars exactly as-is and add ONLY the hero line for route #1 with its share-of-all-flights — captures ~80% of the emotional payoff (the Wrapped/ESPN/data-journalist voices all converge on 'make the runaway leader pop') for a fraction of the work, deferring the diverging-bar change.
- Chord diagram of the top ~12 airports (arcs sized by traffic, ribbons by undirected route count, each ribbon split into directional halves) — genuinely shows the Texas mesh is a network not a list, and is memorable; but it loses exact rank-comparison, is hard to read at card size, duplicates the route-arc map he already likes, and risks landing in the same 'pretty but useless' bucket as the heatmap/choropleth he explicitly dislikes. Better as the MAP card's job than here.
- Add a per-route avg-delay column to turn the leaderboard into a punctuality table ('which of my regular corridors is worst') — actionable for a heavy commuter, but delay data is noisier/sparser and this risks overloading one card; defer unless he asks.

**Why:** The data is a genuinely ranked, heavy-headed magnitude list (AUS-DFW 234, then a steep drop; 62% of endpoints in 5 airports), and for "rank and compare these discrete routes" position-on-aligned-bars is the optimal encoding — six of nine panelists (including the cartographer and the Tufte purist) keep bars for exactly this reason. But three independent voices (data journalist, Bloomberg, creative-coder) all land on the same concrete gap from different angles: the undirected count hides the out-and-back symmetry, which is real and checkable in his own data (Dallas dep 730 / arr 727). And the consumer-delight, ESPN, and journalist voices all want the 13%-of-a-lifetime stat to be the headline rather than buried in row one. The improve path satisfies both convergences without the cost or comprehension risk of a full replace: the hero line delivers the screenshot-worthy emotional hook, the diverging bar delivers the surprising directional finding, and the engine already computes both — so this is additive to a card that is otherwise the model of good progressive disclosure (toggle + drill-through + live metro re-rank) the other ranked cards should match. Replacing with a chord diagram would trade his most-actionable, most-glanceable card for novelty that competes with the route-arc map he already likes and risks the same fate as the heatmaps he rejected.

#### Common layovers `[layovers]` — improve · effort S
**Recommended:** Keep the ranked horizontal BarList, but make it earn its space with two cheap, honest additions that the engine already supports.

(1) A framing dek/headline above the bars that states the ONE true thing the data says: this owner is a nonstop flyer. Verified against his real export: only 160 layovers across ~1,800 flights (~9%), and Dallas alone is 49 of them. Lead line, e.g.: "You almost always fly nonstop — only 160 connections in 1,800 flights, most at home in Dallas." Pair it with one Wrapped-style stat that is computable and true: "~451 hours of your life spent waiting at connections" (sum of every gap; the engine already has depUtcMs/arrUtcMs, just sum B.depUtcMs − A.arrUtcMs). This is the single biggest miss the panel agreed on — the card ranks but never says whether he's a connector.

(2) Per-row second encoding of avgGapMin (already computed) as a thin secondary "typical dwell" strip BEHIND or beside the count bar, plus the existing "~2h35 typical" label, and a sort toggle: by frequency (default) vs by avg dwell. This turns a frequency list into a time-budget tool ("which connection costs me the most sitting time") — Bloomberg/ESPN/IA all converged here, and avgGapMin is the most actionable layover fact.

Encoding specifics: primary bar = count (width ∝ count, current gradient). Secondary = a short muted horizontal pill/strip whose width ∝ avgGapMin on a fixed 0→5h scale (the layoverMaxHours cap is the natural axis), tinted warmer as it approaches 5h so a 4h slog reads visually heavier than a 45-min sprint. Keep the existing row click-through to the connecting flights.

Hard constraint from his real data: cap the inline list at the top ~6 hubs (Dallas 49, CLT 15, Chicago 14, Houston 13, Phoenix 8, Washington DC 6) before the count collapses; everything below is 1-3 connections (14 hubs are count=1). Push the rest behind the existing "See all (35)" overlay. Do NOT add per-hub distribution viz (violin/strip-of-durations) inline — only ~6 hubs have n≥5; for the other 29 a distribution is 1-3 dots, i.e. noise dressed up as signal.

**Alternatives:** 
- Keep exactly as-is (Tufte/journalist/Strava/ESPN minority): ranked bars + the '~typical' sub-label is already correct for a small ordered set; the only mandatory lift is the one-line dek naming him a nonstop flyer. This is the floor — acceptable but leaves the avg-gap dimension and the lifetime-hours hook on the floor.
- Dwell-time scatter/strip: one row per hub, dot at avgGapMin on a fixed 0→5h axis with a count label, sorted by dwell. Surfaces 'which connections cost me the most time' more directly than bars, but loses the at-a-glance frequency ranking that is the card's primary job, and most hubs collapse to the same ~2-3h band — weaker than the dual-encoding bar.
- Per-hub gap distribution (the creative-coder/Strava 'violin' idea): REJECTED for this data. Verified only ~6 hubs have enough connections (n>=5) to show a shape; 14 hubs are a single connection. A distribution viz here would manufacture false texture on 1-3 points and is exactly the kind of novelty-over-truth the owner already rejects (heatmap, density map).

**Why:** Grounded in his actual export, not vibes: I ran the engine's commonLayovers + a gap-distribution probe over the real 1,800-row CSV. Results — 35 hubs, 160 total connections, Dallas=49 then 15/14/13/8/6 then a cliff; 27/35 hubs have count<=3 and 14 have count=1; total dwell ~451h. That shape dictates everything: (a) it's a small ordered set of magnitudes with a vicious long tail, which is the textbook case FOR bars and AGAINST radial/flow/distribution novelty — confirming the keep-the-bars majority; (b) the bare count genuinely hides the second real dimension (avgGapMin, already computed, real variance at the top hubs — Dallas ranges 12min to 4h48), which is the improve-minority's valid point and a near-free add; (c) the dataset's loudest truth — he's a nonstop flyer who barely connects, and has still burned ~451h in terminals — is a sentence the card never says, which both the journalist (headline-first) and Wrapped designer (shareable misery-flex) correctly flagged. The recommendation takes the supported parts of every persona and refuses the unsupported novelty (per-hub violins) because his data literally doesn't have the points to fill them, and he's on record disliking texture-for-texture's-sake (heatmap, density/choropleth maps). It stays glanceable, honest, and keeps the existing faithful flight-pair drill-down.

#### Airlines `[airlines]` — improve · effort S
**Recommended:** Keep the existing logo + bar + count rows with drill-through (they are the right idiom and already click through), but add a thin 100%-stacked horizontal "loyalty share" strip ABOVE the rows that frames the duopoly in one glance: three segments — American 57% / Southwest 33% / Everyone else (38) 10% — each labeled inline (logo or name + %). Encoding: full-width track, height ~22px, rounded; segments colored AAL accent / SWA accent / muted grey; direct labels inside or just below each segment ("AAL 1,026 · 57%", "SWA 588 · 33%", "38 others · 10%"); a one-line dek under it: "Two airlines flew 90% of your life." The "Everyone else" segment and the dek are clickable: the segment opens the long-tail list (the existing See-all popup, which already holds all 40 carriers with bars + drill-into-flights). Then the top-5 logo rows render exactly as today. This surfaces the single most distinctive fact (90% on two carriers) before a 40-row list whose tail is mostly slivers, costs no new chart primitive (a flexbox strip), and keeps every existing drill path intact.

**Alternatives:** 
- Era-share trend strip (the panel's secondary theme): under the rows, a small 100%-stacked bar with 3 era columns — pre-2015 / 2015-19 / 2020+ — showing AAL+SWA share climbing (~55% to ~97%) so the AAL->SWA defection and tail-squeeze become visible. Derivable in-file from model.scoped (year + airlineName). Best if he wants the STORY of consolidation, but higher effort and only meaningful in all-time scope. Prefer a small-multiples era bar over a 40-series streamgraph — a curvy stacked area over 40 carriers risks the exact illegible area-fill idiom he already rejects in the map/heatmap.
- Log/sqrt scale toggle on the existing bars (the 'keep' persona's note), so single-digit tail carriers aren't visually flat next to AAL=1026. Cheap and consistent with the Airports card, but it distorts the headline (a log bar makes a 1-flight carrier look comparable to a 1,000-flight one) — pair it with the share strip rather than use it alone.
- Minimal: leave rows as-is and just add a one-line stat header 'American + Southwest = 90% of 1,800 flights' with no new geometry. Lowest effort; captures the fact but loses the part-to-whole glance the strip gives.

**Why:** The owner's data is a near-duopoly: AAL 1,026 (57%) + SWA 588 (33%) = 90% of 1,800 flights, then a 38-carrier dribble with ~15 single-flight carriers. A 40-row equal-height bar list visually equal-weights a 1,026 carrier and a 1-flight carrier and buries the defining fact. A 100%-stacked share strip states the 90% concentration instantly and honestly (part-to-whole is the truthful encoding for share), which is exactly where 6 of 8 panelists converged ("loyalty meter / dominance strip / share bar"). It also respects his stated tastes: he rejects cell-grids (calendar heatmap) and opaque area fills (density/choropleth) but likes direct, legible encodings (route arcs) — a labeled segmented bar is in that legible family, whereas a 40-series streamgraph is not. Crucially it is additive: the strong existing logo rows and their flight-level drill-through stay, so glanceability goes up with zero loss of explorability.

#### How far `[distance]` — improve · effort M
**Recommended:** Turn the ranked band-list into a TRUE variable-width histogram on a real distance x-axis, keeping the existing editable edges and per-band drill-through unchanged. Concrete encoding: horizontal axis = miles (0 → max flown, ~8,000+; default ticks at the edge values 300/700/1500/3000/6000). Each band becomes a rectangle whose WIDTH = (hi − lo) on that axis and whose HEIGHT (or fill saturation) = flight count, so the eye reads area ≈ "how much of my flying lives in this distance range." The open-ended 6000+ band gets a clipped/hatched right edge labeled "6,000+" so infinity doesn't dominate the axis. Color: keep the existing orange accent gradient; label each bar directly with its count + band range (no separate legend). The editable-edge inputs become drag-or-type bin boundaries positioned UNDER their x-axis tick (still commit-on-blur, still revert-on-invalid). Click a bar → existing overlay.openFlights via flightsByDistanceBand(lo,hi) (unchanged). Add one auto-derived dek above the chart from the modal band, e.g. "Most of your flying is the short Texas hop — under 300 mi." This honestly shows the right-skewed shape: a tall narrow short-haul spike (DFW/DAL/AUS/HOU/SAT, AUS–DFW 234x) and a thin wide long-haul international tail — which equal-height rows currently flatten into visual lies.

**Alternatives:** 
- Minimal-change variant (effort S): keep the vertical BarList rows exactly as-is but (a) add a faint continuous kernel-density / area curve behind the bars showing the true underlying distance distribution through the chosen buckets (Bloomberg/creative-coder convergence), (b) add a median-flight-distance marker line (ESPN), and (c) add the one-line modal-band dek. No axis rework — cheapest way to recover honesty + a 'typical flight' anchor.
- Add a cumulative-% column and a 'You're a short-hop commuter' persona tag to the current rows (Wrapped + Bloomberg) without changing geometry — pure copy/number additions, near-zero risk, but does NOT fix the equal-width distortion the panel's two 'improve' votes care about.
- Leave the card fully as-is (the 6 'keep' votes' literal position) — defensible because the interaction model is genuinely best-in-class, but it knowingly keeps the width distortion and leaves the bimodal short/long-haul story unnarrated.

**Why:** The panel only splits on geometry, not on the model: all 8 personas praise editable edges + drill-through, so those must be preserved verbatim. The real convergence — both 'improve' votes (Tufte, generative artist) PLUS the 'it's effectively a histogram' caveats from Bloomberg, ESPN, and the creative-coder — is that equal-height stacked rows lie about bin width: a 300-mi band and an infinite band render identically. For THIS owner that's the load-bearing defect, because his distribution is extremely right-skewed (Texas-anchored ~64% short-haul, AUS–DFW alone 13% of his life, vs a thin international long-haul tail). A true variable-width histogram is the canonical, non-distorting view of exactly that shape and makes the short-haul spike vs long-haul tail self-evident at a glance. It's 'improve' not 'replace' because the data model, editable edges, and click-through stay intact — only the rendering geometry changes. The single dek (data-journalist + Wrapped convergence) buys the emotional/glance payoff cheaply. This also dodges the owner's stated dislikes: it's not a heatmap, not density-on-a-map, not a choropleth — it's an honest distribution chart he can tune and drill.

#### Intercontinental `[intercontinental]` — improve · effort M
**Recommended:** Keep the ranked continent-pair BarList as the backbone (it already drills pair -> route -> flights and that hierarchy is good), but make NA's hub role and the time-recency that the bars hide explicit. Concretely: (1) Add a one-line hero dek above the bars: "67 intercontinental flights linking 5 continents -- North America to everywhere." (2) On each pair row's value column, append a small muted "first->last yr" chip (e.g. "N.America <-> Europe  37  ·  2008-2026"); this is the journalists' real story (the V-shaped reawakening, NA-Oceania brand-new in 2025) and costs nothing in legibility. (3) Replace the flat top-of-card with a compact NA-centered hub-and-spoke SVG (~120px tall): "NA" node in the center, one spoke radiating to each of EU/AS/SA/OC with stroke width proportional to sqrt(count) and the count labeled at the tip; draw the single AS-EU edge as a thin chord between those two outer nodes since it's the one pair that does NOT touch NA. Reuse the card accent (#6a3cff) and keep every element click-through to the same openFlights overlay the bars already use. Encoding: position = continent, spoke thickness/label = trip count, the off-hub AS-EU arc = the honest exception. This shows the true shape (NA is the hub of 5 of 6 pairs) that a flat bar list discards, without pretending 6 edges (three of them equal 2-count slivers) are a rich network.

**Alternatives:** 
- Pure keep: ranked bars exactly as-is (no chart change), just add the per-pair 'first->last year' chip and the hero dek. This is the S-effort safe choice and is genuinely defensible -- with only 6 pairs and a 37/15/9/2/2/2 distribution, bars are not wrong; the chip + dek capture ~90% of the missing story for almost no risk or build cost.
- Hand-rolled chord/arc diagram (the panel's loud favorite): continents as arc segments, ribbons sized by count. Honest caveat -- on THIS data it degenerates to a NA hub with a few ribbons and three indistinguishable 2-count slivers, so it reads as decoration. Only worth it if 'screenshot delight' is explicitly prioritized over glanceability, and only as an addition above the bars, never a replacement for the drill-down.
- Great-circle 'globe of pairs' mini-map reusing the existing d3-geo/topojson + route-arc renderer he already likes: draw the 6 continent-pair arcs on a small globe, arc weight = count. Ties visually to the route map he rated 'okay' and is geographically honest, but it is M/L effort and overlaps the existing Map card's job.

**Why:** The real export makes the panel's chord consensus mostly wrong for this card: there are only 6 distinct continent pairs and 67 flights (3.8% of all flying), dominated by NA-EU=37, NA-AS=15, AS-EU=9, with the remaining three pairs (EU-SA, NA-SA, NA-OC) at exactly 2 each. NA is an endpoint of 5 of the 6 pairs -- this is a hub-and-spoke, not a mesh, so a chord diagram spends heavy novelty on a structure that has essentially one hub and three identical slivers. Bars already rank a 6-item set correctly and already carry the good pair->route->flights drill-down via BarList; throwing that away for a hand-rolled chord (no chord lib is installed; only d3-geo/topojson) is novelty over honesty. The genuine miss the bars hide is two-fold and both are cheap to fix without changing chart type: NA's hub role (made literal by a hub-and-spoke sketch) and the time dimension (NA-Oceania is new in 2025, AS-NA goes back to 1991, intl flying is a recent V-shaped reawakening) -- captured by a per-row year-range chip and a hero dek. This favors glanceable + drill-downable over decorative, while still giving the convergent panel its 'show what connects to what' via the small hub graphic rather than a misleading dense chord.

#### Geographic extremes `[geoExtremes]` — improve · effort M
**Recommended:** Keep the labeled stat list but anchor it with a small (~140-160px tall, full card width) inset locator map placed ABOVE the rows. Reuse the existing WorldMap stack (geoNaturalEarth1 + geoPath + world-atlas countries-110m land outline, already imported). Plot 6 points: home (DFW, hollow accent ring), and the 4 cardinal-extreme airports + the farthest airport as filled dots. Draw ONE great-circle arc from home to the farthest airport using the existing geoInterpolate/buildArcPath helper, in the same accent (#6a3cff) as the rest of the card. Fit the projection to those points with fitExtent (the WorldMap `fit` mode already does exactly this) so the map auto-zooms to the bounding box of his world — for his Texas-anchored + international data that frames DFW against his far edges nicely. Direct-label only the farthest dot inline (e.g. "DXB 8,000 mi") to avoid clutter; the 4 cardinal dots stay unlabeled but tinted, since the rows below already name them. Keep the existing rows + the "Farthest from home" hero box and their click-through-to-flights behavior unchanged; optionally let hovering/clicking a dot highlight its row (brush-link) but that is not required for v1. Add two tiny derived summary numbers under the map — N–S degrees and E–W degrees spanned (max.lat − min.lat, max.lon − min.lon) — to quantify "how much of the planet" in one glance.

**Alternatives:** 
- Compass-rose mini-map variant: same 6 dots but arranged/annotated around a faint compass rose with the great-circle to farthest labeled with its bearing (degrees from N). More expressive but adds ink and a bearing computation; the locator-map version is cleaner and reuses more existing code.
- Pure list, kept as-is plus only the two N–S / E–W span numbers added (no map). Lowest effort, honest, but leaves the spatial payoff on the floor — and 'farthest from home' as a bare mileage is exactly the abstract-coordinate problem the panel flagged.
- Tiny orthographic globe (geoOrthographic) centered on the centroid of his airports with the 5 extremes + home as dots. Most screenshot-worthy, but a new projection, rotation logic, and back-face culling are extra work for little added legibility over the flat locator map.

**Why:** N/S/E/W extremes and "farthest from home" are inherently spatial — they are coordinates, and a text list strips the one dimension (geography) that makes them meaningful. 7 of 9 panelists (and all 4 'improve' votes) converged on the identical fix: a tiny inset map with the extremes pinned plus a great-circle to the farthest point. That convergence is not novelty-chasing — it adds exactly the missing layer. Crucially it fits the OWNER's stated tastes: he dislikes the calendar heatmap, map density/heat mode, and country choropleth, but explicitly likes the route-ARC map — and this is precisely that idiom (dots + a single great-circle arc, no density, no choropleth). It also fits the data shape: a Texas-anchored hub (DFW/DAL/AUS dominate) with a small, increasingly international set of far edges makes a clean bounding box — few points, no overplotting, so a 140px map is legible where 23-bar lists are not. Risk is low because the implementation is almost entirely already built: d3-geo, topojson-client, world-atlas/countries-110m.json, geoPath, the geoInterpolate-based buildArcPath arc helper, and a fitExtent-based `fit` zoom mode all exist in WorldMap.tsx; the engine's geoExtremes() already returns north/south/east/west/farthest with lat/lon and home defaults to DFW. The list, the hero 'Farthest from home' box, and row click-through to flights all stay, so glanceability and drill-down are preserved.

#### Same metal `[sameMetal]` — improve · effort S
**Recommended:** Add a "hero + rarity" header above the existing ranked bar list; keep the bars as the body. Concretely: (1) A named-character HERO line at top — the #1 repeat tail rendered big with its airline logo: "N137AA — your most loyal airframe, boarded 7 times across 2016-2024 (8 yrs)" (logo from airlineLogos[airlineCode], count from byTail()[0], span computed from min/max of that tail's flight dates). Make it click through to that tail's flights, same as a bar row. (2) A one-line rarity stat-strip just under it: "1,105 tails flown · 796 once · only 309 came back · just 12 flown 5+ times." (3) Below that, the CURRENT BarList exactly as-is (max=5 + "See all (309)"), but add a per-row first→last year span into the existing `sub` slot, e.g. "AAL · 2016-2024" so every repeat row carries the "rode it across N years" texture at zero new layout. Encoding: hero = big number + logo + name; stat-strip = inline counts (no chart); body = unchanged horizontal gradient bars sorted by count. This keeps it glanceable, honest, and fully drill-downable while leading with the only two emotionally true facts in this distribution: one outlier plane and the needle-in-haystack repeat rate.

**Alternatives:** 
- Unit-dot rarity field (creative-coding persona): render all 1,105 tails as a faint grid of dots with the 309 repeats lit in accent and the top ~12 enlarged/labeled, dot click-throughs to flights. Truer to the 'meeting the same plane twice is a coincidence' shape, but heavier to build, harder to scan exact counts, and only worth it if you want one showpiece card.
- Keep current card exactly as-is (Tufte/data-journalist 'keep' position) but add only the single dek line 'You've boarded N137AA seven times' — the minimal-effort version if the hero+stat-strip is too much. Cheapest possible improvement.
- Add a first→last 'tail timeline' sparkline per repeat row (Bloomberg density) — tiny dot-on-a-line showing when in the 2013-2026 window you met that airframe. Adds temporal texture but the BarList sub-row UI isn't built for inline sparklines, so it's more work than the year-span text for similar payoff.

**Why:** Real-data check, not vibes: the owner's 1,557 tail-stamped flights cover 1,105 distinct airframes, 72% flown exactly ONCE; only 309 repeat, only 12 reach 5x, and exactly ONE (N137AA) reaches 7x. So a pure ranked bar list is structurally weak here — below row 1 it's a near-flat wall (213 tied at 2x, 63 at 3x) sorted alphabetically, the 'all bars look identical' failure the panel flagged. Yet the data hides two genuinely delightful, only-a-frequent-flyer facts: a single loyal-airframe hero you keep re-boarding across 8 years, and a striking rarity rate (you almost never meet the same metal twice in 1,800 flights). Three personas who read the actual distribution (Wrapped, data-journalist, creative-coding) converge on exactly this: keep the faithful, click-through bars but lead with the hero and the rarity context. Bars are still correct for the long tail (so this is improve, not replace). Two panel suggestions were tested against the data and dropped: the 'multiple airlines' flag is dead (only 1 repeat tail ever changed airline), and a full dot-field, while truthful, sacrifices scan-ability for a card that already drills perfectly — not worth the cost as the default.

#### Records and streaks `[records]` — improve · effort M
**Recommended:** Keep the stat-callout list as the FORM (these are heterogeneous single-value superlatives with different units — they honestly do not share an axis), but do three targeted upgrades. (1) HERO THE NUMBERS: render the four records as a 2x2 grid of mini trophy cards where the VALUE is the big bold element (e.g. "245" large, "Busiest year · 2016" small above it; "5" large, "Busiest day · 2016-03-14" small), each card keeping its existing click-through to the exact flight subset. Reframe "Longest grounded gap" as "Longest time on the ground" with the day count as the hero number. (2) ANCHOR THE TEMPORAL RECORDS TO ONE THIN LIFETIME STRIP: directly under the grid, render a single ~40px-tall horizontal SVG timeline spanning min->max flight year, with a faint per-year volume baseline (reuse byYear() from aggregate.ts — zero new engine work), then overlay just a few direct-labeled marks: a shaded span for the longest gap, and dots for busiest day/month/year placed where they actually occurred. One axis, single hue, direct labels — NOT a heatmap/grid/choropleth (all three of which the owner has explicitly rejected). (3) ADD THE TWO MISSING RECORDS the panel converged on: a "Busiest week" row (rolling/ISO 7-day max) and a "Longest streak" plus a LIVE "X days since your last flight" counter computed against today. Milestones stay exactly as-is (100/500/1000th flight, click-through to the single flight).

**Alternatives:** 
- KEEP AS-IS (Tufte/IA/data-journalist position): leave the thin stat rows untouched — they are honest, scannable, and already have the best drill-through in the app. Lowest risk; the legitimate fallback if effort is constrained. Cost: under-sells genuinely trophy-worthy numbers (245-flight year, a 35-year arc).
- TROPHY GRID ONLY (no timeline): do upgrade #1 and #3 (2x2 hero cards + week record + live streak) but skip the lifetime strip. Captures the Wrapped/ESPN delight and the missing streaks at lower effort and avoids any temporal-encoding risk, but leaves the gap/streak as context-free numbers — the one weakness 4+ personas flagged.
- INLINE SPARK PER ROW (Bloomberg position): keep thin rows, append a tiny per-row sparkline/bar (busiest year as a highlighted bar within the year series, gap as a span). Densest option, but it scatters the same year-series across four micro-charts instead of one legible strip and reads as clutter on a delight card.

**Why:** The owner's data has trophy-grade superlatives (a 245-flight year, 5 flights in one day, a 35-year arc that craters at COVID) that the current thin rows bury — so the 4 "improve" personas (Wrapped, ESPN, Bloomberg, Strava) are right that the FORM is fine but the SELL is weak. Yet the 4 "keep" personas (Tufte, IA, data-journalist, creative-coder) are also right that these are unlike-unit scalars that must NOT be crammed onto one chart and that the existing per-record drill-through is the app's best and is untouchable. The recommendation threads both: list-of-heroes form (honest, no forced shared axis, drill-through preserved) + ONE thin single-axis lifetime strip that only the inherently-temporal records (gap, streak, busiest day/month/year) attach to — which is precisely the one record every persona agreed is weakest as a context-free number. Critically it respects the owner's hard nos: this is a single-axis time-LINE with direct labels and arcs-of-time aesthetics he likes, not the heatmap, density map, or choropleth he has explicitly called useless/annoying. The week-record and live streak are the highest-value cheap adds (the literal "most ESPN thing missing") and the year series is already computed by byYear(), so the timeline needs no new engine code.

#### Delays `[delays]` — improve · effort M
**Recommended:** Keep the big on-time % hero and the click-through "Most delayed" list, but add a CENTERED delay-minutes histogram between them. Encoding: x = arrival delay in minutes binned and zero-centered (e.g. bins ≤-15 | -15..0 | 0..15 | 15..60 | 60..120 | 120+), with a vertical zero line; bars left of zero (early) in a cool/teal hue, bars right (late) in the existing pink→purple ACCENT_GRAD, the on-time band (-15..+15) subtly shaded. y = flight count, bar height/length encodes frequency. Direct-label two marks: a median tick (data is ~-1 min, i.e. EARLY) and the single worst-delay flight as a labeled outlier dot/tail at the far right (the 393-min flight). Build it as horizontal CSS-div bars (the app's existing bar idiom) or a tiny inline SVG — no scale lib needed; the signed per-flight delayMin field already exists. Under the hero, add an honest coverage line: "delays known for N of M flights" (only rows with both scheduled+actual gate-arrival have delayMin). The on-time % definition (≤15 min) stays as the hero number.

**Alternatives:** 
- Horizontal jitter/strip plot of every counted flight's delayMin on a -30..+120 axis (one dot per flight, on-time band shaded, worst tail flagged) instead of binned bars — shows the raw long-tail asymmetry more literally but is denser and less glanceable than buckets.
- Add a secondary 'on-time rate by year' sparkline/mini-line under the histogram (Strava/Bloomberg ask) to answer 'did my reliability degrade in the dense 2015-19 years?' — good but secondary; gate it on sufficient yearly coverage so sparse early years don't mislead.
- Spotify-Wrapped emotive add-on: a single 'lifetime hours lost to delays' stat (sum of positive delayMin) as a small callout — cheap, relatable gut-punch, complements rather than replaces the histogram.

**Why:** The owner's delay data is strongly right-skewed: a majority of flights arrive EARLY (median ≈ -1 min) while a tiny number of brutal delays (one 393-min outlier) drag the mean to ~+8. "On-time rate + avg + worst" as flat text actively lies about this shape — an average is meaningless on a distribution this asymmetric, which is why 6 of 8 panelists independently converged on a delay-minutes distribution, most of them explicitly centered on zero. A centered histogram is the one chart that shows the truth (most flights cluster near/just-before zero, with a long thin late tail) at a glance. It keeps the genuinely strong parts the panel agreed to keep — the screenshot-worthy on-time % hero and the already-correct drill-to-FlightDetail most-delayed list — so this is an honest, glanceable, drill-downable upgrade, not novelty. By-airline/by-route splits were rejected as primary because delay coverage is only a subset of the 1,800 rows and would thin out unreliably; hence the explicit coverage disclosure instead. This card also dodges the three viz types the owner has explicitly called useless (calendar heatmap, density heat, country choropleth).

#### Shortest flights `[shortest]` — improve · effort M
**Recommended:** Keep the ranked-list idiom (it is correct for named extreme records), but merge Shortest + Longest into ONE "Flight extremes" card and upgrade it three ways. (1) Direction toggle: a Shortest <-> Longest segmented control sitting next to the existing distance|duration toggle — same engine call extremeFlights(scoped, metric, dir, 50), just expose `dir` to the user instead of hard-coding it per card. (2) Hero stat: promote the #1 row into a big headline above the list — e.g. "Shortest: AUS->DFW · 182 mi · 55 min · 2017-03-14" with the route + value oversized, so the card has one glanceable takeaway (esp. the 0-mi RPJ skydiving "land where you took off" oddity, which extremeFlights already preserves). (3) Inline scale: add a thin per-row length bar (reuse BarList's value/peak width math — width = miles/peakMiles) behind each "date · FROM->TO ... value" row so relative magnitude is visible at a glance, with the shortest row a sliver and the longest nearly full. Rows stay click-through to Flight Detail, "See all (N)" overlay stays. Net: two 87-line identical twins collapse into one richer card, removing a redundant tile.

**Alternatives:** 
- Minimal-effort keep: leave Shortest as a standalone ranked list but add the one-line hero dek on the most-extreme flight only (data-journalist's 'ONE thing'); skip the merge. Lowest risk, still fixes the 'no takeaway' gap.
- Side-by-side his-and-hers (Wrapped designer): one card showing shortest AND longest simultaneously as two mini route arcs scaled to true relative length, tiny hop next to the marathon. More visceral but needs arc geometry and doubles vertical space; higher effort, less scannable than a list.
- Sortable dense table (Bloomberg): convert to a real multi-column table route | miles | duration | date with click-to-sort headers. Adds power-user scanning but is heavier than the data warrants for a top-N=5 card and clashes with the dashboard's BarList visual language.

**Why:** These are specific named flight records the owner wants to read row-by-row (route, date, value) and drill into — an object-level list, not a distribution. That is exactly why a time axis, heatmap, or choropleth would be wrong here, which matches the three viz modes the owner has already told us he finds useless. 7 of 8 panelists independently affirm the list idiom. The real defect is structural, not idiomatic: ShortestCard.tsx and LongestCard.tsx are byte-for-byte near-identical (same Rows, same SeeAll, same metric toggle, opposite `dir`) — merging them is a clear, panel-converged density win that deletes a redundant tile. The hero stat gives the card the single glanceable takeaway it currently lacks, and surfaces the genuinely screenshot-worthy 0-mile RPJ flight the engine deliberately keeps. The inline length bar reuses the existing BarList value/peak pattern, so relative scale (the one thing a bare list hides) costs almost nothing to add.

#### Longest flights `[longest]` — improve · effort S
**Recommended:** Keep the ranked flight list, but make the ranking VISIBLE and give it a headline. Concretely: (1) Hero row at top — the single longest leg rendered big (font-display ~22-26px), e.g. "DFW->DEL · 7,500 mi · ~16h" with date, on the ACCENT_SOFT pill the GeoExtremesCard already uses for "Farthest from home"; tag it with a small "personal record" marker. (2) Below it, the top-5 rows get an inline horizontal length bar per row, normalized to the #1 (the current selected metric = bar width), reusing the existing chunky gradient bar-track styling from BarList. Bar width encodes distance-or-duration; the numeric value stays right-aligned and tabular. This is the one encoding 4 of the analytical panelists independently asked for, and it's the payoff for THIS data: the longest-flight distribution has a tiny head of intercontinental hauls (DEL/DOH/LHR ~4,500-7,500mi) sitting far above a dense ~1,000mi domestic pack, so a bar makes the eye instantly feel the outlier gap that a bare number column flattens. (3) Keep the distance/duration segmented toggle and keep each row click -> openFlight detail (aircraft, airline, tail already live in FlightDetail, so rows stay clean). (4) Keep the "See all (N)" overlay; show bars there too.

**Alternatives:** 
- Pure keep (Tufte/Pudding/ESPN/IA/Strava baseline): leave it as the bare ranked list, no bars. Honest and faithful, but leaves the outlier-gap story on the floor that this specific data is begging to tell.
- Merge Longest + Shortest into one 'Flight extremes' card with a min/max toggle (Bloomberg + Wrapped pitch). DRY-correct since the two cards are already byte-identical twins of extremeFlights(...,'long'/'short'), but a single toggle hides half the answer behind a click and kills the 19-min-hop-vs-transoceanic-haul CONTRAST that is the merge's own best selling point. Only do this if dashboard card count must shrink.
- Add a tiny great-circle thumbnail per row (creative-coder pitch) showing the haul's reach on a mini globe/projection. High 'wow' for an HNL/DEL leg, but it's L-effort, fights the glanceable density goal, and duplicates the route-arc map he already likes; skip unless the bar lands and he wants more.

**Why:** The data is a top-N of individual extreme flights — inherently ranked list data, not aggregate — so 6 of 8 panelists correctly say keep the medium. The improvement is forced by the owner's actual distribution: a sharp head of a few intercontinental legs over a long flat domestic pack means the magnitude of "longest" is invisible in a number-only column. An inline length bar + a hero on the #1 makes the ranking legible at a glance and gives the card the headline the data-journalism lens demands, while every row still drills to the faithful flight detail (the IA lens). It deliberately rejects novelty (no globe thumbnails) and rejects the merge, because the dashboard is a scroll gallery where seeing longest and shortest as separate at-a-glance cards beats hiding one behind a toggle. This also keeps Longest/Shortest as symmetric twins — apply the identical change to ShortestCard.

### KEEP (2)

#### Countries and states `[countries]` — keep · effort S
**Recommended:** Keep the ranked flag-bar list with expandable US/IN/MX state breakdown and drill-through (it is genuinely the right encoding for a US-dominant, long-tail country->state hierarchy) and harden it with three cheap additions, none of which change the core viz: (1) HERO LINE above the list: "N countries · M US states" as a single bold stat (Spotify-Wrapped anchor; the data has very few distinct countries so the brag is passport breadth, which only lands as a headline number). (2) A DEK sentence under the hero, direct-labeling the actual skew this card hides: "But ~95% of your flights never leave the US" (compute share = US count / total country-touch count). (3) A per-row FIRST-VISIT YEAR BADGE on each country row (e.g. a muted "since 2007" or "'07-'24" trailing the count), computed from the f.year already present on every EnrichedFlight in stats.ts byCountry - this is the one real gap the panel found, because it surfaces the late-international arc the static rank flattens, at near-zero cost. Encoding details: bars stay LINEAR (do NOT log-scale - log lies about how lopsided his travel is, and US dominance IS the honest headline the dek states in words); US row expand should lead with TX so the in-card 64% Texas read is immediate; keep state-promotion (splitCountriesByState) and the existing onRowClick drill-to-flights exactly as-is. Implementation touches: extend CountryEntry in src/engine/stats.ts with firstYear/lastYear (track min/max f.year per country during the existing loop), and render hero+dek+badge in src/app/cards/CountriesCard.tsx.

**Alternatives:** 
- Cumulative-countries-over-time mini step line (Strava's ask): a small monotonic line of distinct-countries-reached vs year, collapsed under the list or on hover. Honest and on-data (internationalization is real) but it is a second chart competing for a dimension with very few distinct countries; the first-visit-year badge delivers most of the same temporal signal inline for a fraction of the effort, so prefer the badge and only build this if he wants a dedicated temporal read.
- Replace bars with a choropleth/map: explicitly REJECTED - the owner has already said the country choropleth is annoying and map heat/density is useless; a ranked flag list is more glanceable for ~a dozen countries and more honest about a 95/5 magnitude split than a fill can be.
- Log-scale the bars (Tufte's optional note): rejected - it would visually flatten the very US-dominance that is this card's true story; keep linear and state the skew in words via the dek instead.

**Why:** The dimension is a small-cardinality, severely US-dominant, long-tailed country set with a clean nested country->state hierarchy (US/IN/MX) - exactly what a ranked flag-bar with progressive disclosure is built for, and 7 of 8 panelists independently said keep with the same structure. A map/choropleth is the obvious novelty temptation and the owner has explicitly rejected it twice, so honesty and glanceability both point back to the list. The bars handle magnitude truthfully (US dwarfing everything is the point, not a bug to hide with log-scale); the expand handles the TX-64% granularity; the existing drill-through keeps it explorable. The only genuine miss the panel converged on is temporal - his international travel is a LATE-phase signal that a static total rank erases - and that is fixable inline with a first-visit-year badge using the f.year already on every flight, plus a hero number and one dek sentence to anchor the brag and name the 95/5 skew. Maximum honest value for minimum new surface area.

#### Aircraft `[aircraft]` — keep · effort S
**Recommended:** Keep the ranked brand to family to type drill-down bar list (BarList in AircraftCard.tsx), but tighten it with three cheap, honest additions: (1) a one-line dek above the bars naming the dominant pair, e.g. "Boeing 737 + Airbus A320-family = 2 of every 3 jets you've flown" (computed: 737 ~740 + A321 242 over ~1,800 = ~64%); (2) a "% of flights" value alongside the raw count on each brand/family/type row (tabular-nums, right-aligned) so the bar length is backed by an exact share; (3) at the family level, show the intra-family type split inline (737 family -> -800 vs -700) which the expand subRows already render -- just add the % there too. Encoding stays: count on x (zero-baseline horizontal bars, sorted desc), brand as top rows, family as the expand tier, exact type on click; every row continues to drill through to the underlying flights. This preserves the honest zero-baseline ranking and glanceable scanning the owner values while adding the headline + share the journalism/density panelists asked for.

**Alternatives:** 
- Zoomable icicle (NOT treemap) of brand>family>type: a single horizontal-partition figure where width = count, click a band to zoom one level deeper. Honest proportional widths, keeps a readable left-to-right reading order, and drills like the bars. Better than a treemap because 52 long-tail types become readable nested bands rather than unreadable confetti rectangles. Use only if the owner wants ONE nested figure instead of expandable rows.
- Stacked 100% composition bar per brand (one slim bar per brand split into its families/types) as a compact 'fleet mix' strip above the ranked list -- shows the 737-800 vs 737-700 vs MD-80 share at a glance without re-sorting. Low effort, complements rather than replaces the bars.
- Optional nostalgia callout: a small 'retired metal' badge on dead types (MD-80, 203 flights) -- emotionally strong per the Wrapped/journalism panel, but requires new curated retirement-year data and is orthogonal to the core viz; ship only if that reference data is added.

**Why:** The data is a true 3-level hierarchy (2-3 brands -> a few families -> 52 types) with extreme dominance (737-800/700 ~740, A321 242) and a long single-digit tail -- a ranked bar with an expand/drill toggle is the cleanest honest encoding for exactly this shape, and 7 of 8 panelists independently said keep. Bars give a zero-baseline, exact counts, and fast top-down scanning; the existing expand-then-click already delivers progressive disclosure and flight drill-through (the interaction architect called it a model card). The one dissent (treemap/icicle) buys proportional area but loses the readable baseline and buries the long tail in tiny rectangles -- precisely the "pretty but unreadable geometry" the owner has already rejected in the heatmap, density map, and choropleth. The real gap the panel converges on is narrative, not chart type: a headline naming the Boeing-narrowbody dominance and a % share column. Those are additive and cheap, so the card stays a bar list and just gets honest context.

## New stat / card ideas (ranked)

1. **Career arc: flights-per-year with shaded life-phases** (effort M) — Full-width line/area of flights per year 1991-2026 with auto-detected phases shaded and directly labeled: Sparse (pre-2013), Ramp (2013-14), Peak road-warrior (2015-19, 170-245/yr), COVID crater (2020, 76), Plateau (2021-26). Pin 3 annotations: 2016 peak (245), 2020 trough (76), latest-year-vs-peak callout. Clicking a year/band sets the global scope filter and can open those flights. This is the single biggest missing chart and a replacement intent for the disliked heatmap.
   - _Reveals:_ The owner's 13-year working life as one legible shape: rise, peak, COVID cliff (69% drop), and the fewer-but-farther plateau. Position over time, not opacity.
   - _Feasibility:_ Fully computable. byYear() already exists; date/year on every row. Verified: peak 2016=245, 2020=76, span 1991-2026 (23 yrs). Phase detection is a simple slope/threshold heuristic.
2. **Day-of-week signature (the business-traveler fingerprint)** (effort S) — A 7-bar week strip Mon..Sun with Saturday highlighted as negative space, plus a derived weekday-vs-weekend split and a one-line verdict ('You almost never flew Saturdays — the day you kept'). Real data: Wed 330, Mon 308, Tue 304, Thu 296, Fri 268, Sun 202, Sat 92. Optionally show how the split bent in the post-2020 plateau (more leisure?).
   - _Reveals:_ The loudest behavioral pattern in the whole dataset — a commuter, not a tourist — which NO current card touches. Both a Tufte/data-journalist 'notice this' chart and a tender Wrapped beat.
   - _Feasibility:_ Trivial and verified exactly (Sat=92). Day-of-week derives from the date field with zero tz ambiguity. No new engine work beyond a one-liner reducer.
3. **Your Flight Personality (one-line persona verdict)** (effort M) — A single derived-archetype finale card computed from the data shape, e.g. 'The Texas Triangle Commuter' — 90% two airlines, 64% Texas endpoints, modal hop under 500mi, Wed-peak/Sat-dead, +8min mean but median-early. One bold sentence + 3 supporting micro-stats, styled as the screenshot card. Pick the archetype via a small rules ladder over already-computed stats.
   - _Reveals:_ Synthesizes the entire dataset into an identity — Wrapped's most-shared moment is always 'here's who you are.' No card attempts a top-level persona.
   - _Feasibility:_ Computable: all inputs (airline share, Texas share, modal distance band, DOW peak, delay median/mean) already exist or are trivial. Effort is mostly copy/rules design, not data.
4. **13% of your life on one route (AUS-DFW hero stat)** (effort S) — Hero single-stat card: AUS⇄DFW flown 234 times = 13% of every flight ever, with a repeating tiny-arc motif and a comparison line ('more than your bottom 100 airports combined'). The Routes card buries this as row one of ten; promote it to its own reveal.
   - _Reveals:_ The single most extreme concentration fact in the data — how anchored his life is to the Texas triangle.
   - _Feasibility:_ Verified exactly: AUS-DFW=234, =13.0% of 1800. byRoute() already returns this as the top row; just reframe as a hero stat.
5. **Airline loyalty over time (AAL vs SWA, eras stacked)** (effort M) — 100%-stacked area of airline share by year: AAL | SWA | the field. Annotate the crossover/loyalty-shift years and SWA's mid-decade surge. Brushable: drag a year range to re-scope, click a band to drill. Replaces the static airline ranking's blind spot.
   - _Reveals:_ Two carriers own ~90% (AAL 1026, SWA 588) but the MIX moves with job/role changes — a consolidation-and-defection story the flat lifetime ranking cannot tell.
   - _Feasibility:_ Fully computable. byAirline() exists; just add a year dimension. Verified AAL 1026 / SWA 588 / field ~186. Stacked-area is the only new render primitive.
6. **Death of the MD-80 (you outlived the Mad Dog)** (effort S) — Single big nostalgia stat: 203 flights on McDonnell Douglas MD-80 family aircraft — a type American fully retired in 2019 — with a sepia/retro treatment, optionally over a small per-year ridge showing it rise (~54 in 2015) then vanish by 2019. 'Your logbook records an era of aviation ending.'
   - _Reveals:_ An emotional time-capsule unique to a long-tenured AAL flyer: he's a witness to a retired fleet. The aircraft cards hide this entirely.
   - _Feasibility:_ Verified: 203 MD-80 family flights (MD-83 106, MD-82 67, MD-80 29, MD-90 1). aircraftType + year on every row; trivial filter. aircraftClass already exists for grouping.
7. **Hours of your life aloft (and gate-waiting), in human units** (effort S) — Two lifetime totals in relatable spans: total time in the air as 'X full days at altitude' (and work-weeks / movies-watchable), and total layover/gate-wait time as 'Y days waiting at gates.' Big numbers, human units, with a thin in-air-vs-calendar-span proportional bar.
   - _Reveals:_ Converts abstract minutes into the visceral life-cost of the road — emotionally resonant for a heavy traveler weighing home-presence with a newborn arriving. The odometer does miles, never time.
   - _Feasibility:_ Computable: durationMin exists on every flown row (with documented fallbacks); layover gaps already computed in commonLayovers via depUtcMs/arrUtcMs. Note: durationSource mixes actual/estimate, so label as approximate.
8. **Rolling 30-day flight count (intensity as a trajectory)** (effort M) — A continuous trailing-30-day flight-count line across the whole timeline, with the all-time peak window labeled and a horizontal reference line. The explicit, position-based replacement for the disliked calendar heatmap — answers 'how hard am I traveling now vs my worst stretch.'
   - _Reveals:_ True travel intensity as a shape (grind peaks, 2020 collapse, recent throttle-back) using position not color-opacity — directly addressing the owner's stated dislike of the heatmap.
   - _Feasibility:_ Computable from sorted dates alone; a simple windowed counter. No tz needed. Replaces IntensityCard's data with a far better encoding.
9. **Directional out-and-back balance (top routes split by direction)** (effort M) — Diverging bars centered at zero for top corridors: forward vs reverse leg counts (DFW->AUS 119 vs AUS->DFW 115), sorted by absolute imbalance. Each direction drills to its flights. Routes are currently undirected; this is a genuinely new cut.
   - _Reveals:_ Near-perfect symmetry ('you always come home', ~96% balanced) plus the rare one-way imbalances that flag relocations, repositioning, open-jaw international, or missing-return data gaps — all mathematically invisible in undirected counts.
   - _Feasibility:_ Verified: AUS->DFW 115 / DFW->AUS 119. Directed counts are a one-line change to byRoute (drop the frozenset). Drill reuses existing flight-filter + Overlay.
10. **Trip reconstruction (multi-leg journeys, not flights)** (effort L) — Cluster consecutive flights into trips using the same depUtcMs/arrUtcMs gap logic the layovers card already computes, then list trips ('DFW->LHR->DXB, 3 legs, 18h') each expanding to its legs on a mini-map. A new navigational primitive the whole app currently lacks.
   - _Reveals:_ Travelers think in trips, not isolated legs. This is the biggest missing primitive — it lets layovers, intercontinental, and records all be re-expressed per-trip, and turns the international pivot into 'real journeys.'
   - _Feasibility:_ Computable: the layover/gap clustering already exists; extend it to chain legs into trips. Caveat: pre-2008 sparse rows and missing times make some clusters ambiguous; needs a gap threshold + same-day heuristic.
11. **New-airport discovery curve (still exploring vs grinding)** (effort M) — Cumulative distinct airports over time (2 in 1991 -> 161 in 2026) as a step-line, with per-year first-visits as bars. Highlight that the curve never flattens (+12-14 new airports in 2023-24) despite lower total volume.
   - _Reveals:_ Distinguishes exploration from routine: even in the low-volume plateau he keeps hitting new airports, meaning recent travel is more varied per-flight than the high-volume Texas-shuttle peak — reframes a 'settled' traveler as a perpetual explorer.
   - _Feasibility:_ Fully computable: first-seen year per airport from (date, fromCode, toCode); 161 distinct airports confirmed. byAirport() infra exists. Simple cumulative reducer.
12. **On-time reality: median-early vs mean-late** (effort S) — Centered arrival-delay histogram with divergent median and mean lines: 52.7% arrive EARLY, median -1 min, yet mean +8.2 min due to a 393-min worst case. The gap between the two lines IS the insight.
   - _Reveals:_ Exposes how a single 'average delay' number lies about a right-skewed distribution. Upgrades the existing DelaysCard from a number to a shape.
   - _Feasibility:_ Computable: delayMin already on every row; delayStats() exists. Only a histogram render + median/mean overlay is new. Caveat: delay only present where actual times exist (older rows blank).
13. **International share over time (the reawakening abroad)** (effort S) — Per-year sparkline/100%-strip of the international fraction (flights touching a non-US airport). Intl collapsed near 0% during the 2015-16 Texas grind, then climbed to a ~13-year high (~22%) in 2024 — the data fingerprint of the COO role going global.
   - _Reveals:_ Makes the known 'Texas-domestic -> international' career arc a visible directional trend instead of one lifetime ratio. Pairs naturally with the career-arc card.
   - _Feasibility:_ Computable: continent/country on resolved endpoints; intercontinental() and domestic tiers already exist. Just add per-year aggregation. Some pre-resolution rows may be unresolved.
14. **Streaks and busiest windows (the ESPN cut)** (effort M) — Longest run of consecutive months with >=1 flight, longest consecutive-days-with-a-flight streak, current active streak, and the busiest 7-day window ('busiest week: 9 flights'). Frames intensity as a chase against his own past.
   - _Reveals:_ Streaks are the most absent-yet-natural stat for a logbook; reframes the grind as records. Complements the rolling-30-day card with discrete milestones.
   - _Feasibility:_ Computable from sorted dates only; no tz. RecordsCard exists as a home for it. Define 'consecutive days' carefully (a streak likely means consecutive flying-days, not calendar days).
15. **Hub gravity / home-range rings** (effort M) — Nested concentric rings: share of flights that stay intra-state, intra-country, intra-continent, vs intercontinental, plus the killer single stat — share of ALL flights touching the home metro (DFW/DAL). Each ring drillable. Collapses three redundant 'close to home' cards into one radius gradient.
   - _Reveals:_ Quantifies the inescapable Texas anchor (DFW=965, DAL=492) as ONE legible number/figure and consolidates the SuperDomestic trio — 'how tethered am I to home?' as a first-class drillable question.
   - _Feasibility:_ Computable: domesticTierOf() + byAirport() + home-metro key logic all exist. Verified Texas dominance. Mostly a re-encode/consolidation, low data risk.
16. **Same-airframe reunions (tail-number time gaps)** (effort S) — For tails flown more than once (309 of 1,105), rank the longest elapsed gaps between boarding the same physical aircraft ('You boarded N3CL again 6 years later'), plus the most-met metal (N137AA, 7 times). Upgrades SameMetal from a static count to time-aware serendipity.
   - _Reveals:_ A delightful temporal-serendipity stat the current same-metal count can't express: time between encounters with the literal same plane, across a 35-year span.
   - _Feasibility:_ Verified: 1105 tails, 309 repeats, N137AA=7. byTail() exists; just add max-gap-between-dates per tail. Trivial extension.
17. **This year vs your average (pace tracker)** (effort M) — For the in-progress year, flights/miles/hours to date vs same-date pace in an average year and vs the peak year, as a gauge or projected-finish bar ('2026: ~56 flights by late June -> on pace for ~115, vs avg, vs 245 peak').
   - _Reveals:_ Strava-style 'are you ahead of yourself' framing turns a passive log into a living scoreboard against his own history — a quantified-self hook entirely absent today.
   - _Feasibility:_ Computable: byYear() + day-of-year pacing. Caveat: only meaningful for the current year and needs a sensible 'average year' baseline definition.
18. **The long tail, made honest (concentration / Lorenz curve)** (effort S) — A single cumulative curve: airports ranked on x, cumulative % of flights on y, annotating the 50% and 80% crossing points and the 29 once-visited airports. A one-figure Gini-like 'how Texas-centric am I.'
   - _Reveals:_ Quantifies extreme concentration in one curve instead of a bar list that hides the long tail as slivers — and honestly shows both the dominance and the breadth.
   - _Feasibility:_ Computable: byAirport() gives the ranked distribution directly; cumulative sum + two threshold annotations. Verified heavy concentration (top 5 Texas airports dominate).
19. **Cross-filter chip bar (global brushing backbone)** (effort L) — A persistent filter bar beside the year dropdown: click any airport/airline/aircraft/country anywhere and it becomes an active chip that re-scopes EVERY card at once; chips stack and clear individually. Reuses existing flight-filter helpers and the scoped-model pipeline.
   - _Reveals:_ Not a stat but the missing IA backbone: today the only shared filter is year, so you can't ask 'show me everything about my Austin travel' across cards. Brushing/linking makes the dashboard one coherent explorable instead of 23 isolated views.
   - _Feasibility:_ Feasible but cross-cutting: requires threading a shared filter through useModel/scoped-model and every card's drill. Highest integration risk of the set; high leverage if done.
20. **Bearing rose / directional grain (which way you fly)** (effort M) — A polar wind-rose binning each leg by initial great-circle bearing from origin, petal length = flight count, optionally split inbound/outbound. A radial, cyclical encoding that suits this near-symmetric hub-and-spoke data.
   - _Reveals:_ The directional grain of a hub life — heavy E-W North-America spokes vs the rare long NE transatlantic / NW transpacific legs — pure flow-direction signal no card captures, and a fresh geometry distinct from the route-arc map he already likes.
   - _Feasibility:_ Computable: initial bearing from endpoint lat/lon (distance.ts already has the geo math). Lower priority because it's more aesthetic than actionable, and only resolved-endpoint flights qualify.

## Completeness critic

**Under-exploited data dimensions:**
- DAY-OF-WEEK (departure weekday). Fully derivable from f.date and already-present depUtcMs; never aggregated by any card. His real data: Wed=325, Tue=298, Mon=302, Thu=291 vs Sat=91 (a 3.5x trough). This is the single cleanest 'business traveler' fingerprint in the whole export and zero cards touch it. WhenYouFly only does hour-of-day, not weekday.
- TRIPS / JOURNEYS as a unit. Every card treats the atom as a single flight LEG. There is no concept of a trip (a chain of consecutive legs away from home and back). The engine already orders flights chronologically and computes layover gaps (commonLayovers) and a day-trip-turnaround test, so trip segmentation is nearly free. Without it the dashboard cannot answer 'how many trips', 'avg legs per trip', 'longest trip', 'time away from home', or 'multi-leg vs nonstop' — arguably the most natural way a road-warrior thinks about his own travel.
- CARRIER DEFECTION OVER TIME. Airlines is a lifetime ranking only. The real arc is dramatic and unshown: SWA is ~0 before 2015, crosses AAL in 2016 (115 vs 126), and in 2025-26 SWA actually OVERTAKES AAL (33 vs 16, then 24 vs 17). The 'two-airline life' is really a loyalty SHIFT in progress. depUtcMs/year are present; only an aggregation + stacked-area/line is missing.
- RED-EYE / OVERNIGHT flights. depUtcMs and arrUtcMs already encode this. His data has 105 departures in 22:00-04:59 and 48 flights whose landing date differs from takeoff date (crosses midnight). A named, emotionally-resonant category ('overnighters') that is currently invisible — WhenYouFly buries it inside an opacity-free histogram but never labels or lists it.
- DIRECTIONAL / COMMUTE BALANCE. byRoute is undirected by design. The directional data shows a near-perfect commute symmetry (716 departures from Dallas metro = 716 arrivals; AUS->DFW 119 vs DFW->AUS 113). 'You always come home' / out-vs-back balance per route is computable from fromCode/toCode and is never surfaced — the home-range / homecoming story the panel gestured at but nobody pinned to the directional numbers.
- NEW-AIRPORT / FRONTIER discovery rate. First-seen year per airport is trivial from the ordered flights. His curve is a real story: exploration spikes (2010=14, 2013=13, 2016=13) and a recent resurgence (2023=14, 2024=12) layered on a routine-heavy core. 'Exploration vs routine' over a career — completely unbuilt. Cumulative-unique-airports-over-time would be the Strava 'map filling in' artifact.
- FLIGHT-NUMBER identity. Only tail numbers (sameMetal) are aggregated. flightNumber is 100% populated and has strong repetition (AAL1448 x20, AAL1561 x15, AAL1373 x14). 'Your flight' — the specific scheduled service you ride most — is a fun, distinct dimension from 'your most-flown airframe' and is unused.
- BEARING / DIRECTION-OF-TRAVEL and great-circle geo-derivations. Full endpoint lat/lon is present (haversineMi already uses it) but bearing, compass-rose of travel, equator/date-line crossings, and N/S/E/W net displacement are never computed. geoExtremes only does min/max lat/lon + farthest-from-home. The cartographer's 'spatial substrate spent only on text stats' critique is literally true in the code.
- SEASONALITY as an explicit (anti-)finding. Month-of-year pooled across years is strikingly FLAT (123-175, no summer or holiday spike) because it's business travel. byMonth exists but is only used in the heatmap; the flat-season fact ('no vacation season') is a headline the data hands you for free and no card states it.
- TERMINAL/GATE data. Dep Terminal (67%), Dep Gate (67%), Arr Gate (63%) columns exist in his CSV but the parser doesn't even ingest them. 'Your home terminal/gate at DFW' is a hyper-personal touch that's currently dropped at the parse step (parse.ts reads PNR/Seat/Cabin but not the terminal/gate columns).

**Missing cards:**
- **Your week (day-of-week rhythm)** — A 7-bar (or radial 7-spoke) card of flights by departure weekday with a one-line dek. His data screams it: Wed 325 down to Sat 91. Direct-label the peak and the dead day. This is the highest-signal, lowest-effort missing card and the cleanest proof he's a Mon-Thu business flyer. Pairs with hour-of-day to give a full weekly heartbeat without any opacity encoding he hates.
- **Trips (journeys, not legs)** — Segment consecutive legs into trips (new trip when you depart home after being home; reuse the chronological ordering + layover logic already in commonLayovers). Show: total trips, avg legs/trip, longest trip (by legs/miles/days away), nonstop vs connecting share, and total days away from home. Click a trip to see its legs on the existing subset mini-map. This adds the missing structural unit the whole dashboard lacks and reframes 1,800 legs as N journeys he actually lived.
- **Career arc (flights per year over time)** — ONE annotated year-over-year line/area of flights per year, 1991-2026, with callouts on the 2015-16 peak (245), the 2020 COVID crater (76), and the rebound. This is the temporal SPINE seven of the eight panelists independently demanded, and it's the narrative replacement for the Intensity heatmap he calls useless. byYear already returns exactly this series; only the viz is missing. Make the year-scrubber on the route map derive from it.
- **The airline switch (AAL vs SWA over time)** — A stacked-area or two-line trend of AAL vs SWA vs other share by year. The story is a real defection: SWA from ~0 (pre-2015) to overtaking AAL in 2025-26. Turns the static 'two-airline life' ranking into a loyalty narrative. All data present (airlineName + year); the time aggregation is the only new piece.
- **Home & away (directional commute balance)** — Replace/augment the undirected routes with a directional home-base view: departures-from-home vs arrivals-home (716/716 = 'you always come home'), top outbound vs inbound routes, and net displacement. Satisfies the panel's 'home-range rings' idea with the actual directional numbers and answers the road-warrior's real question: how balanced is my commute.
- **Frontier (new airports over time)** — Cumulative-unique-airports line plus new-airports-per-year bars, annotating exploration spikes (2023=14, 2024=12) against the routine core. A Strava-style 'map filling in' / explorer-vs-commuter card. First-seen-year is a trivial pass over the already-ordered flights.
- **Overnighters (red-eyes)** — A small named card: count of red-eye departures (105 in 22:00-04:59) and cross-midnight flights (48), with the list click-through. Uses depUtcMs/arrUtcMs already on each flight. A distinct, screenshot-worthy category currently invisible inside the dep-hour histogram.
- **Flight personality / wrapped finale** — A single synthesized closing card that names his characters from data already computed elsewhere: top route (AUS-DFW, ~13% of all flying), most-ridden airframe (sameMetal #1), most-flown scheduled service (AAL1448 x20 - NEW, from flightNumber), home airport, dominant aircraft (737), peak year. Sequenced as one bold reveal. The Wrapped/ESPN 'give the data a face' payoff the dashboard completely lacks today.

**Gaps the panel under-explored:**
- DATA-AVAILABILITY BLIND SPOT (the biggest collective miss): the panel proposed cabin-class, seat-preference, and business-vs-leisure ideas implicitly, but in HIS actual export Cabin Class, Seat Type, Flight Reason, and Notes are 100% EMPTY (0/1800). Seat is only 21% and PNR 41%. Any card keyed on cabin/fare/trip-purpose is impossible for the primary user. Conversely the panel ignored fields that ARE present and dropped at parse time: Dep/Arr Terminal & Gate (63-67%). Nobody audited which columns are actually populated, so several suggestions are undeliverable and one real dimension was overlooked.
- Every persona pushed TIME-as-encoding (sparklines, arcs, year toggles) but treated it as a styling change. None confronted the structural prerequisite: there is no per-flight sub-day timestamp carried onto EnrichedFlight (aggregate.ts has an explicit TODO about this) and, more importantly, no trip/journey object. The 'add a sparkline everywhere' advice underestimates that the highest-value temporal stories (trips, time-away-from-home, commute cycles) need a new aggregation layer, not just a chart swap.
- WEEKDAY was almost entirely missed by the whole panel. Only the Strava persona mentioned 'Mon-Thu / Sat dead' in passing, and even then proposed no card. Given how clean the signal is (Wed 325 vs Sat 91) and how trivially it's computed, this is the most underweighted concrete opportunity in the room - the panel over-indexed on grand reframes (arcs, maps, rings) and under-indexed on the single best cheap win.
- Several personas said 'collapse the three Close-to-home cards into one' and 'kill the heatmap/choropleth' - but that is consolidation/removal, not new insight. Net of those subtractions, the panel proposed very few genuinely NEW cards backed by specific numbers. The strongest additive opportunities (trips, weekday, carrier-defection trend, frontier/new-airports, commute balance, repeated flight-number) were each touched by at most one persona and never costed against what the engine already computes.
- Nobody distinguished 'lifetime aggregate that should stay lifetime' from 'aggregate that's lying by hiding a trend.' Delays, for instance: on-time % is shown as one number, but punctuality almost certainly varies by airline, by airport, by year, and by hour - the Bloomberg persona gestured at sortable on-time-by-airline but no one noted that delayMin is only present on ~92% of rows (timed flights) and absent on the sparse early years, which would bias any per-year delay trend. The data-quality caveat on every proposed trend was never raised.
- International-vs-domestic GROWTH over time was asserted by multiple personas ('recent climb into international', 'rise of international flying') as a headline, but no one verified it against the data or proposed the specific card. The classify/continent machinery exists; an intl-share-by-year line is buildable and would either confirm or refute the claim the panel kept repeating - it should be tested, not assumed.
- Map ideas got heavy attention (edge-bundling, H3 hex, Dorling cartogram, time-scrub) but all are high-effort rewrites of the one card he already says is 'okay' in arc mode. The panel over-invested design energy in the surface that's working and under-invested in the many bar-list cards that are genuinely interchangeable - the better move is fewer, smarter non-map cards (weekday, trips, arc) rather than three competing reinventions of the map.

## Panel cross-cutting big ideas

- **Tufte data-viz purist:** The dashboard has ONE structural data fact it keeps fighting: extreme concentration on a 35-year sparse-to-peak timeline. Two airlines are 90% of flights, five Texas airports dwarf everything, and one peak year (245) is 245x a sparse year. The default chart everywhere is a linear ranked bar list — which is honest for the top 3 rows and then collapses the entire long tail into invisible 1-pixel stubs. Tufte's prescription: (1) log-scale or axis-break the dominance cards (airports, aircraft, airlines) and add an explicit 'tail rollup' so the 29 once-only airports are accounted for rather than dropped; (2) convert every binary/few-category 'close to home' card from a ranked bar LIST to a single 100%-stacked proportion bar — they are part-to-whole, not rankings. Second big idea: the two 'creative' cards (intensity heatmap, when-you-fly) both encode quantity as OPACITY, the exact distortion the owner intuitively rejects — replace opacity-as-quantity with position/length on a shared axis (small-multiple yearly sparklines for intensity; one overlaid 24h line for dep-vs-arr). Third: the richest untapped story is TIME — almost every card is a lifetime aggregate, yet this is a 13-year career captured month-by-month. Sparklines cost almost no ink and would turn static scoreboards (overview, airlines, domestic/intl) into the trend narrative the data is begging to tell: the rise of international flying, the AAL/SWA trade-off, the 2020 dip. Less chartjunk, more layered comparison over time.
- **Spotify-Wrapped consumer-delight designer — I judge every card by one question: will this make Vijay stop, feel something, and screenshot it? His data has gifts begging to be dramatized: 13% of his entire flying life is ONE route (AUS-DFW, 234x), 90% on just two airlines, 64% Texas, 203 flights on a dead aircraft (MD-80), and a 35-year arc that craters at COVID. Ranked bars hide all of that. I want big bold single-stat reveals, narrative sequencing, and emotional hooks over neutral tables.:** The dashboard is built like an analytics tool (ranked bars everywhere) when his data is begging to be a STORY. Through the Wrapped lens, three moves transform it: (1) Lead every card with ONE bold derived stat before any list — his data is wildly lopsided (13% on one route, 90% two airlines, 64% Texas), and ranked bars deliberately flatten exactly the asymmetry that makes him feel something; the dominance IS the insight. (2) Sequence and personify — add a 'Flight Personality' finale, name his most-loyal airframe and most-flown route as characters, and replay the map chronologically; Wrapped works because it paces reveals and gives data a face. (3) Kill the analytical-but-cold viz he already rejects (heatmap, heat/choropleth map modes) and replace them with narrative equivalents — his year-by-year counts aren't a heatmap, they're a career arc with a visible COVID crater; tell it as a story he lived through, not a grid of opacities.
- **Data journalist (NYT-Pudding). Every chart needs a headline and an annotated insight; story-first, guided reading, direct labeling. I keep asking: what is the ONE thing this card should make you NOTICE? The owner's data is not abstract — it is the 13-year shape of a working life: a Texas grind that peaked 2015-16 (245 flights/yr), an AAL/SWA duopoly that crowded out everyone else, an MD-80 fleet that lived and died on his logbook, and a recent climb back into international. Cards that just rank without a sentence are leaving the story on the floor.:** 1) This dashboard ranks superlatives but rarely tells the TIME story — yet time is where the biography lives. Half the cards (intensity, airlines, intercontinental, countries) sit on a 23-year span and almost none plot a trend line. The owner calling the heatmap 'useless' is the symptom: he wants to see his career arc, not a grid of opacity. Add a thin time axis to the cards that have one and the dashboard goes from 'stats about me' to 'the story of me.' 2) Almost every card is missing its dek — the one-sentence headline that says what to notice. The data hands you the sentences for free ('two-airline life,' 'you always come home,' 'no vacation season'). A ranked bar list with a headline is journalism; without one it's a spreadsheet with rounded corners. 3) There are too many bare-count 'close to home' cards (state/country/continent) doing part-of-whole work that a count list does badly; collapse them into one nested proportion bar — concentric rings of home-ness — and you both reduce clutter AND make the proportion legible, which is the entire point of those cards.
- **Bloomberg-terminal density designer — glanceable info density for a power user: tables, inline sparklines, sortable leaderboards, no wasted space, fast scanning, numbers you can act on.:** 1) The single biggest waste in this dashboard, through a density lens, is FRAGMENTATION: ~23 cards where one concept is split across many (Shortest/Longest are one sortable table; the three 'Close to home' cards are one concentric stacked bar; Aircraft/AircraftClass overlap; Odometer is a chip not a card). Consolidating could drop the card count by ~6 while INCREASING information per screen. 2) The data has a hard temporal spine (1991-2026, the 2015-19 peak, the 2020 cliff, the recent international pivot) that almost NO card uses — nearly everything is an all-time aggregate. Adding time as an encoding (sparklines in rows, rolling-pace line replacing the disliked heatmap, stacked-area trends) is the highest-leverage change and it directly fixes the owner's two pet peeves (calendar heatmap, map heat) by replacing opacity-as-quantity with position-as-quantity. 3) The owner is a power user who wants numbers he can ACT on, but the dashboard is mostly vanity totals; the punctuality, layover-dwell, and carrier/route asymmetry data already computed in the engine should be promoted into sortable scorecards (on-time % by airline and by route, avg layover dwell, commute imbalance) so the dashboard becomes a decision tool, not a trophy case.
- **Cartographer / GIS visualization expert — flow maps, projections, proportional symbols, density, cartograms, time-animated movement:** The dashboard has exactly ONE map and it tries to be three maps at once via a mode toggle — and two of the three modes (heat, choropleth) are cartographic anti-patterns for THIS data. SVG-Gaussian-blur 'heat' is a glow, not a density estimate, and it ignores projection area distortion; a whole-country choropleth on a 95%-CONUS flyer is a single red blob crippled by MAUP. The owner's instincts are correct. The fix is to lean into what this dataset actually is: a hub-and-spoke FLOW network with extreme Texas concentration and a thin but growing international fringe. That means edge-bundled great-circle arcs with proportional-symbol nodes as the hero, an equal-area hex/H3 or geoAlbersUsa binning for density, and a per-STATE choropleth or Dorling cartogram instead of country fills. Second big idea: the data carries a fully spatial substrate (endpoint lat/lon, bearing, continent, IANA tz, UTC instants) that's currently spent only on a few text stats — bearings, centroids, great-circle-vs-flown efficiency, equator/date-line crossings, and timezone-span are all computable today and are far more on-lens than another opacity heatmap. Third: keep the ranked bar lists for routes/airports/countries — they are the correct precise-comparison tool, and geography should COMPLEMENT them, not duplicate them; the mistake would be mapifying everything. Map the flows, bar the rankings.
- **ESPN sports-stats analyst — I treat a flight log like a career stat sheet: leaderboards, streaks, head-to-head, season-by-season arcs, milestone chases, and the goofy derived superlatives that make a box score fun. My north star: every number should feel like it's competing against another number or against the player's own past self.:** Through an ESPN lens this dashboard is a deep, well-built BOX SCORE but a thin HIGHLIGHT REEL — it's loaded with counts and rankings yet almost devoid of the things that make stats feel like a game: streaks, head-to-head rivalries, season-over-season arcs, and 'on pace to' chases. Three concrete shifts: (1) Kill the redundancy and the modes the owner hates — three 'close to home' bar cards collapse into one home-range rings viz, and the map should be arcs-only. (2) Respect the data's extreme shape: it's wildly long-tailed (DFW=965 vs ORD=67) and duopoly-dominated (AAL+SWA ~90%), so flat linear bars flatten the real competition below the giants — use log scales, dominance strips, and 'vs the field' framing. (3) Add a time/career dimension everywhere: the single best upgrade is replacing the hated intensity heatmap with an annotated career-arc chart (dynasty 2015-19, 2020 collapse, rebound), and beefing up Records with the streaks and rivalries that turn a stat sheet into a story.
- **Interaction information architect — I judge the dashboard as one explorable system: how you drill from a summary into the underlying flights, how selections in one card relate to others, whether progressive disclosure and click-through are consistent, and whether navigation between related views is coherent. I read the real code (Overlay stack, BarList, WorldMap, registry) and the owner's actual data shape.:** 1) The app has a superb DRILL-DOWN spine (stacked overlay, openFlights/openFlight/openList, back-nav, subset mini-map) but almost no CROSS-FILTERING: the only shared state is the year dropdown. Click-through is depth; brushing/linking is breadth — and breadth is entirely missing. Given how Texas/AAL/SWA dominate, a global chip-based cross-filter ('show me all my Austin / all my Southwest') would be the single highest-leverage addition, turning 23 sibling cards into one explorable system. 2) Two surfaces violate the 'everything drills down' contract the rest of the app teaches: the MAP (zero click handlers — the most spatial card is inert) and OVERVIEW (inert hero numbers). Fixing these, and making the map the brush source, would make the spatial and headline layers first-class navigation instead of dead-ends. 3) Progressive disclosure is inconsistent: some cards expand inline (BarList Show-more) while others open a 'See all' popup, and four 'Close to home'/intercontinental cards slice ONE nested home-distance hierarchy without ever showing that nesting. Pick one disclosure idiom app-wide, and collapse the home-distance tiers into a single segmented, hierarchy-aware card so related views read as a coherent story rather than scattered look-alikes.
- **Quantified-self Strava designer - I judge every card by whether TIME is the hero. The strongest story in this 1,800-flight, 35-year export is a life-phase arc (sparse 1991-2012, ramp 2013-14, peak 2015-19, COVID collapse to 76 in 2020, plateau 2021-26) plus a carrier defection (AAL-dominant -> SWA surges 2015-16) and a Mon-Thu business signature (Sat is dead at 92). The dashboard almost never plots a metric against time, which is its single biggest miss.:** Through the time lens, this dashboard is a beautifully built ATLAS pretending to not be a DIARY. It excels at 'where/what' (maps, rankings, fleets) but almost never plots a metric against time, yet the export's most powerful story is purely temporal: a 35-year career arc with a clear 2015-19 peak, a measurable COVID collapse (245->76), a carrier defection, and a Mon-Thu business heartbeat. Three moves would transform it: (1) add ONE hero year-over-year trend chart (the arc) - it's the missing spine of the whole product; (2) make 'this year vs your average/peak' a recurring Strava-style frame so the log becomes a living scoreboard rather than a museum; (3) wherever a card shows a lifetime total or ranking (overview, airlines, delays, countries, domestic split, map), offer a 'by year' toggle so trajectory is always one click away. And kill the two visualizations the owner already rejects (map heat, choropleth) - replace that toggle real-estate with a year-scrubber on the arc map, which would become the app's signature artifact.
- **Generative creative-coding artist — I judge every card by whether its geometry tells the truth about the data's *shape*. With 62% of endpoints in 5 airports, a 35-year span, near-perfectly symmetric out-and-back routes, and a Mon-Wed/evening business rhythm, this data wants radial, cyclical and flow encodings — not 23 stacked bar lists that all look identical.:** 1) This dashboard is 18 bar lists wearing different hats. With one dominant home (DFW), one duopoly (AAL+SWA), one body type (737), and 62% of endpoints in 5 airports, every ranked-bar card produces the same silhouette — a tall first row and a fade-out. The data's real richness is structural (a Texas route mesh), cyclical (weekday + hour-of-day + month rhythms), and temporal (a 35-year career arc), and almost none of that is encoded geometrically. Trade ~4 redundant bar cards for 3-4 radial/flow figures (a route chord, a time spiral, a 24h clock, concentric rings of home) and the same data suddenly has variety and memorability.

2) The owner's three dislikes (heatmap, density map, choropleth) share one flaw: they spread a highly-concentrated, long-tailed dataset across uniform cells/areas, so 62%-in-5-airports renders as bland uniformity. The fix isn't 'less creative' — it's encodings whose geometry MATCHES concentration and cycles: arcs/ribbons that get fat where traffic is heavy, spirals/clocks that wrap on the natural period. The original calendar heatmap failed not because radial-thinking is wrong but because a grid is the one creative form that erases his data's shape.

3) Every cyclical or nested dimension is currently drawn linearly, breaking the very structure it represents: hour-of-day as a 0-23 bar (cuts the midnight wrap), three nested geographic shells as three separate flat lists (hides the nesting), 35 years as a flat grid (flattens the arc). Match topology to geometry — cycles become rings/clocks/spirals, nesting becomes concentric/icicle — and the cards stop fighting the data.
